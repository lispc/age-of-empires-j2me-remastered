# aoe/ai — 玩家 AI

规则式玩家 AI 包。接口 `PlayerAi.tick(c game)` 每帧首在 paint/Timer 线程被调
（c.java 帧首 hook，`-Daoe.playerAi=<全限定类名>` 反射装载；装载失败/tick 异常
打 `[ai]` 日志并禁用，不影响游戏）。hook 在 onPaint 帧首，**不受 screenState
门控**——弹窗态（ss==2）下模拟停走但 AI 照样被调，AI 须自按 -7 关窗。

两个实现：`RuleBasedAi`（随机图，见下）与 `CampaignAi`（战役，见末节）。

## CampaignAi（战役，2026-09-04）

`-Daoe.playerAi=aoe.ai.CampaignAi` + `-Daoe.dev=campaign:N`（N=1..7 对应
missionIndex 0..6）。批跑：`tools/camloop.sh -m N -n 局数 -t 超时 -k`。
战役地图每局重掷（z=98 结算重掷种子），camloop 无需种子参数。
各关胜负条件：反汇编考证在 `docs/research/campaign-mission-scripts.md`
（工具 `tools/scriptdis.py`）。

成绩（camloop 各 5 局，turbo+BFS，全图读敌表——战役期初许可，诚实化是后话）：

| 关（missionIndex) | 任务 | 成绩 | handler |
|---|---|---|---|
| #0 拆堡 | 拆敌 TC | **5/5**（~3k tick） | tickRaze |
| #1 护送 | 送 slot0 村民进堡区，死 1 村民=负 | **5/5**（v2 环路护送 ~2.3k tick；取代砍树墙 ~96k，快 42×） | tickEscort |
| #2 经济 | 木/金/石各 >100（严格） | **5/5**（前置矿仓后 ~11.7k tick；矿仓前 28k，看门狗修复前 2.2M） | tickGatherQuota |
| #3 拆家 | 拆光敌 5 建筑 | **5/5**（~4.6k tick） | tickRaze |
| #4 科技 | 升城堡时代+放置大学 | **19/20**（v40：intercept 攻击态敌拦截 + earlyquota 兵营前 2木1石；演进见下"待攻/分流复验"） | tickCastleRace |
| #5 守城 | 扛 5 波刷兵后全歼 | **5/5**（~5.9k tick） | tickProtectCastle |
| #6 总攻 | 拆光敌 13 建筑（Keep 塔环+19 兵） | **5/5**（~7.3k tick，v8） | tickFinalAssault |

- **#4 翻案**：初判"原版 bug 胜利链自锁"是错的——tf[14]（大学可建标记）在我方
  **放置**大学时由 c.java:7427 `techFlags[10+n2]=0` 清零，胜利链
  "升城堡(tf14=1)→放大学(tf14=0)→50t→胜"成立。初判只 grep 了字面量
  `techFlags[14]`，漏了变量索引写。**data.res 不需要修补**。
- **#4 待攻（2026-09-05 更新，v24 后 6/20，相位 pin 同相位成对定案）**：
  唯一敌开 AI 的关（Easy 参数：采集 ×1、攻击阈值 30、**免费资源滴 1000**），
  敌 TC(5,26) 真运营且无限续兵；我方 2 村民 10/10/10 白手起家，**村民 4 人是
  游戏硬上限**（unit-stats.md）。败因结构：竞速物理极限 ~11k tick（胜局
  9.3-13.3k local），躲不掉敌 ~8k 起大波；剩余败因 = **早死桶 ~30%**（首波
  ~1.2k local 压到 TC，塔 ~2k 才立、长枪被 W12/G12 门卡住）+ 9k 后消耗战。
  **排除项**（别再试）：塔先于兵营（v11）/三塔（v16）/采集走廊回避（v5/v15
  两次实锤更糟）/全免门出兵（v13）/委托 RuleBasedAi 全套（v12：RB 在全雾
  战役图停滞，0/5 且死得更快）/矿场驻塔（v17 mine 0/10——塔离 TC 无人守门
  早亡）/竞速裸奔砍塔 2（v17 bare 2/20，不优于双塔）/塔前禁兵或金门抬 18
  （v19/v20 均 0/10——早期长枪是首波承重墙）/采矿场排到塔后（v25 tower1st
  0/20——交存点晚了金/石断供，全线 5.5-6.8k 早亡）/围攻放宽到塔圈外
  （v29 8/20+1 stall 回滚——中盘拉村民离岗打游击反而饿死经济，且慢速
  村民追移动 raider 永远差 1 格形成追逐僵局）/WT 提前救拉锯（v30/v31：
  锁槽修复后 WT 真能研出，但城堡前置未齐时副业科技抽干 20/20/20 竞速金
  →9/20→7/20；加"城堡竞速锁"（城堡未研时副业科技只在 35/30/35 以上投）
  回到 9/20——拉锯负局根本攒不到锁门，WT 救不了它们，只给已胜局
  提速 232-592t）；rush 不可行（通用 TC
  拆毁胜利在战役 missionIndex≠0 被跳过，c.java onThingDestroyed case 9——
  #4 只有大学一条路）。**当前配方**（默认路径，C4V 空）：双走廊塔 +
  战中不停摆（塔满编后 invader 不冻结竞速链）+ 塔圈逃命豁免（d2≤25 不逃）
  + kite-flee（塔圈内贴身敌短跳塔背侧 2 格）+ 塔前兵帽 1 + 村民围攻
  （v26：塔未立且无兵时 ≤2 敌 raid 全体村民围攻不逃——同相位成对
  9/20 vs 6/20，翻转 +3/反向 0）+ 时代科研储备（v27/v28：塔立后补兵门
  抬到下一时代成本+5，剃头兜底按原门补 1——同 9/20 但胜局平均快 1-3k，
  根治"长枪门 12/12 < 封建 15/15/15 军费饿死科研"的拉锯死循环）。**败因测绘**（emil 遥测+相位 pin）：
  早死相位 = 敌首波 ~1.2-1.5k local 到 TC（塔 ~2k 才立的时间差），
  存活相位首波 ~2.5k+。**下一候选**：首波预警再提前（敌兵营落成即
  预攒塔料）；剩余早死相位（0/91/112）围攻也救不回（连续波磨光村民）。
  camloop 透传 `C4V=mine|bare|tower1st` 可回放证伪变体。
- **#4 分流复验（2026-09-06 阈值批测期，9/20 相位 pin 精确复现）**：20 局分流
  =早死桶 2（phase 0/112）+ **中盘竞速带 9（6.4-8.3k 全灭，胜局全在 8.8k+）**。
  中盘病理（game9 vs game16 逐事件对账）：raider 露营矿线在塔圈外（塔 (37,52)
  到石矿 (41,40) d²=160>25）→ 村民逃命循环（单局 flee=186 次）→ 收入崩 →
  **封建 15/15/15 永远攒不齐（败局全程 0 科研；胜局 FEUDAL@4209→CASTLE@7561
  →大学@8225）** → 敌 ~8k 大波先于大学落地。两个候选变体同相位成对证伪：
  `C4V=swarmcamp`（露营 raider≤2 围歼扩展——败局存活 +1-4k tick 但零翻胜，
  还引入"村民存活无产能"新僵局：CampaignAi 投降门只认 0 单位）9/19+1僵；
  `C4V=tower3`（塔 3 罩采矿场，v16 老结论在当前配方下重测）恰 9/20。
  两变体均留 C4V 门内不采纳。剩余候选=文档内"敌兵营落成预攒塔料"+feudal
  攒料优先级（配额禁区，慎动）。
- **#4 攻克（v40，2026-09-06 第二轮分流，9/20→19/20 同相位成对定案）**：
  胜局 ticks 从 8.8-13.4k 压到 **6.1-8.4k**——大学在敌 ~8k 大波前从容落地，
  竞速不再撞波。两个默认采用件：
  - **intercept（默认开，`C4V=nointercept` 关）**：TC 12 格外的**攻击态**敌
    （正在打矿线/外塔的）→ 派最近 ≤2 长枪去换。mid-band 病理的对症药：露营
    raider 被清 → 矿线恢复 → 封建/城堡竞速不再饿死（game9 全程 0 科研 →
    18 局全程 6.1-7.4k 干净落地）。边界设计：只打**攻击态**（已接敌不会风筝，
    v29 证伪的是"追移动 raider"）；只派兵不派村民（swarmcamp 证伪：村民
    1 换 1 亏）；已攻击态的我方兵不动（retask 风暴禁律）。
  - **earlyquota（默认开，`C4V=noearlyquota` 关）**：兵营前配额 3木1石→
    **2木1石**——原 3木要凑满 4 村民才轮到采石工，塔 1 的 16S 等 4 号村民
    （game9 塔实际 2696t）；2木1石让 3 号村民就上石，塔 1 提前 ~300-400t。
    phase 0 直接翻胜（LOSS@2816→WIN@6998），其余 18 胜无反复。
  - 同轮证伪（均留 C4V 门）：`campmark`（蹲守标记滚动窗 10 格+1500t——0 增益
    3 新僵局 2 翻负：敌波每次经过都重标记全片区域=竞速反复中断；多种类回退
    按"第一正缺口"把金/石工赶去砍柴。**教训：躲避式方案治不了"要清场才能
    竞速"的局**）；`repair`（修塔移植 9/19+1僵——无增益，胜局本就不靠塔血）。
  - 残留：phase 112 一局（earlyquota 下存活 2928→8007 仍负）。
- **批测方法论（2026-09-05 相位 pin 起）**：战役地图恒定（res 103-109 种子
  字节全非零），批间方差 = 菜单导航墙钟漂移造成的进关相位；camloop 默认
  每局 `-Daoe.devPhase=(i-1)*7`，同相位必同结果，A/B 变体逐相位成对对比
  （翻转计数即结论，不再吵抽样噪声）。注意：相位 pin 会暴露 AI 节流器的
  绝对 tickCount 假设——nextDecide 在菜单阶段涨到 ~3200，拨钟后恒冻结
  （已在 CampaignAi/RuleBasedAi 加回溯检测）。
- **#6 攻克（v8，2026-09-04 深夜，5/5 平均 7330t）**：终审配方 = 废除野战
  直攻塔 + 反应式威胁（质心 12 格内最近敌兵全军集火）+ 就地轮换（威胁源
  7 格外站桩回血，绝不回家——回家往返 700t 在敌投石机集火下=永久添油）+
  孤立塔全员进场/集群塔近战圈外戒备。塔区守军由集火就地问杀（攻城器
  56/发两三轮一个）。反面教材（v5-v7）：计划性野战 17 兵换 19 守军全看
  遭遇阵型（存活方差 2-10）；攻城器进野战=送锤（16t/格逃不掉）。
- **战役机制教训**（都踩过坑，写代码前先读）：
  - 采集一载 = 102 tick 计时（任务字高字节倒数），期间写 slot[7]=0 = 清零
    重来——重发节流必须远大于 102t（escort v1 用 24t 永远砍不倒树）。
  - 单位占位（0x2xx）会盖住格下的资源/地形显示，扫描地图要避开或豁免。
  - 可走判据 = `(t & 0xFFF) == 0`（虚空 0x0/雾 0x8000/已探索 0x4000 都能走；
    雾下资源 0x83xx 低位非零=挡路）。
  - 战役敌 aiEnabled=false：守军不追远、不出击，只原地自动接敌——
    "守军最少优先+打了就跑+站桩回血"是拆建筑关的通关公式。
  - 站桩回血要 pos==tgt：撤退点必须按槽位散开（同点撤退互相占位永不回血）。
    回血速率 +1 HP/2t（tickUnits case 0：任务字 0 + pos==tgt + (tickCount&8)!=0，
    HP<255；全员 HP 上限 255）。
  - 塔/守军瞄准 = 槽序扫描（aimProjectiles/acquireTarget 都取射程内最低 slot，
    不取最近/最弱）——我方剑士 slot 0-4 < 攻城器 slot 10-16，近战进场即天然
    肉盾；单位战斗态不追人（tickUnits case 1 只对 slot[5] 格原地开火），贴身
    接敌=双方互锁（acquireTarget 双写）；被打会报复（resolveAttack 把受害者
    tgt 改写成攻击者位置）——**报复会追击 5-6 格**（2026-09-06 m6 二轮勘误，
    "守军不追击"只对未接敌成立；近战威胁半径仅 ~2 格）。另：闲置远程只
    自动索敌建筑，对单位仅相邻触发（站桩弓兵对单位零输出）。
  - 伤害公式统一 攻<<4/甲：塔 64/甲 per 17t（Keep [46]=4）、投石机对甲2 目标
    112/发；单位拆建筑同式（resolveAttack case 256，建筑甲 [33+type]，HP 255）。
  - 通用"拆敌 TC 即胜"规则在战役 missionIndex≠0 被跳过（onThingDestroyed
    case 9）——#4 这类关只有脚本胜利一条路，rush 不可行。
  - 交存锚点 hdr[8]=TC/hdr[9]=伐木场/hdr[10-11]=采矿场；无 TC 关要伪造
    hdr[9] 到袋内空地，否则载满回送 orbit。
  - 敌开 AI 的运营关（#4）：逃命=102t 采集计时清零，敌露营矿线时一刀切
    逃命=收入归零——解法组合 = 塔位罩住矿线 + 塔圈内不逃 + 兵/塔资源
    排序（塔前兵帽 1，军费不能抢走塔的启动金）。
  - **僵尸局**：敌 tickAi 的威胁扫描/stance 推进以我方单位为引
    （`if (n7!=0)`）——我方 0 单位时敌军永不动工，TC 站着也没人拆，
    units=0 永不终局（实测 t=25M）。批测侧解法 = AI 投降（exitOnResult
    下 0 单位+无产能路径持续 500t → [result] LOSS 退出，CampaignAi.tick
    首部）。另一形态 = handler 把卡死单位当"在忙"（#2 经济关 phase-0：
    村民永不到达=永不 idle=永不再分派）——解法 = 位置漂移 ≤1 格且 600t
    的看门狗强制重派 + handler 内补村民训练。

## RuleBasedAi

随机图（gameMode=0）Easy/Medium/Expert 通用。**2026-09-04 第六批起默认开
「迷雾诚实模式」**（`-Daoe.aiFog=0` 回退全图，ailoop `-f` 透传；`AOE_AIFOG=res|tc`
为消融诊断档）：只读已探索格（mapTiles 0x8000 位=唯一视野层，与渲染/小地图
可见性逐格一致，考证见 game-mechanics「迷雾与视野」）上的敌单位/建筑/资源，
禁读 hdr[1] 统计字段（军值/资源/人口/TC 位），敌 TC 位置靠侦察记忆。
成绩（诚实模式，ailoop -b -t 240，种子 1000+ n=10，1004 退化图跳过）：
**Easy 8/10+7/10（两批合并 15/20）；Medium 8/10+4/10（两批行为等价、合并
12/20——同种子跨批翻面剧烈，只信合并带）；Expert 1-2/10（四批 5/40）**。
全图对照（-f 实测 4/10，胜种子与 v56 完全一致=回退路径零失真）：
Easy/Medium/Expert 全图历史成绩 5/5、10/10、4/10。信息价值量化与消融见
`docs/research/rulebased-ai-medium-iteration.md`「信息收紧（第六批）」——
**全图挂的总价值 ≈ Expert +2.5-3 胜/10、Medium +2-4、Easy +1-2；没有任何单一
信息通道能独力补回 Expert 缺口（res 消融 2/10、tc 消融 1/10），硬约束是波 1 前
的探索速率（单位视野只有 3×3）**。

**阈值矩阵批（2026-09-06，原版阈值恢复后）**：Easy 进攻阈值恢复原版 50
（e3747d3）后重标定。批测基建：`-Daoe.aiAttackThreshold=N` 覆盖钩子 +
`ailoop -T`（阈值矩阵 50/100/150/200 通吃验证）。**⚠️ 批数必须带 `-b`（BFS）：
无 BFS 口径完全不可比**（2026-09-06 事故：无 -b 跑出 Easy 0/5 伪基线，带 -b
同种子 4/4+1僵）。修复探针活锁后成绩（种子 1000+ n=10，诚实+`-b`）：
**T=50: 10/10、T=100: 10/10、T=150: 8/9+1僵、T=200: 9/10（合计 37/40）；
Medium（官方 T=60）8/10、Expert（官方 T=100）1/10 均在原带内=零回归**。
本批机制（全部阈值无关或随阈值连续缩放，无硬编码分支）：
- **探针活锁修复（本批最大兑现）**：引擎对不可达/被占位路点即时取消移动
  （pos==tgt→被视作 idle→下拍重发），原推进条件 `slot[2]==wp` 在到达后不存在、
  800t 超时门永远够不着——实测同一路点每 8t 重发 3800 tick 永不推进（seed 1000
  t=1504-5280 卡死 (25,46)），全图资源开图瘫痪=木饥荒图慢性死亡。改按"同一路点
  累计重发 400t=走不到"跳过；probe-pull 块另配独立游标（与 idle 探针共写一个
  游标会 6/16 环两套路标互相踩）。
- **波次窗口模型（阈值感知）**：`waveTick = 1000 + aiAttackThreshold×22`
  （敌 all-in 触发=敌军值 ≥ 阈值且 > 我军值×0.8，tickAi:8543 实读；首波到时
  实测 T=50→~2.3k、T=100→~5.6k、T=150→~5.0k）。aiAttackThreshold 是难度面板
  公开参数（玩家选难度即知），不属 hdr[1] 隐藏信息，诚实模式可读。
  消费者：**时间窗闪击 TIMERUSH**（敌 TC 已知 + `t+1500 < waveTick` + 兵≥5 且
  军值不劣 → 全军直扑敌基地杀村民断收入；高阈值=首波远+敌军事积累期真空，
  T=150 实证 peak=0 整整 4.6k）。
- **资源侦察 RES-SCOUT**：敌 TC 找到后军事侦察本会停摆，木/金整类不可见时
  保持 1 侦察兵走全环螺旋（村民探针限前 6 环半径 ≤13 够不到远林）。
- **woodStarve / 僵尸投降**：木<5+无木工+无可采木=生产链死→带现有部队 all-in
  （静默窗 4k 防误判）；无军事+产能永不可恢复（收入死断可达性推导，非阈值
  枚举）持续 500t → `[result] LOSS` 契约认输（exitOnResult 批测专用，
  seed 1000 型 21M tick 僵局的出口）。
- **围城逃生门 + 收入死断裸成本放行**：塔补不起且军事归零时允许战中重建兵营；
  金/木收入死断时训练门按裸成本（5/5）放行——家底花光好过锁死（G=5/W=5
  差一口气的磨局实证）。

历史（全图时代 v56 定型 = v35 微操四件套 + 围城经济组件）：Expert 1000+ 4/10、
1010+ 1/10；Medium 1000+ 10/10、1010+ 10/10、2000+ 7/10；Easy 1000+ 5/5。
第五批结论：败局定性为"石/兵种帽死锁"，胜负的完美预测子 = 有没有建成
archery/smith。六颗常败种子（1002/1005/1006/1007/1009/1010）跨 v35-v56 未胜
=石贫图/近距 rush 图对 4 村民经济的可行性边界。

### Expert 全图攻坚（2026-09-06 第 41 夜，aoe.exp* 旋钮批）

基线（-b -f，相位 pin）：1000+ 3/10、1010+ 1/10。**败局签名：全在 ~2.3-2.9k
首波窗口**——敌 ×8 经济 train-1,军值 ~1.2k 过阈值 100,首波 12-18 单位;
我方 3-4 塔+5-7 兵守得住 = 波全灭 → CRUSHED → 反杀 TC(胜局全走这条);
守不住 = 我军被歼 → 村民被猎 → 崩盘。胜负手 = 首波窗口的**塔后杀伤率**。
- **EXP_MANGONEL 转正（`aoe.expMangonel=0` 回退）：两带 4/20→8/20**。内容 =
  Expert 铁匠铺提到射箭场前（门槛 30/22→25/15）+ t8 生产门 40/40→25/25。
  塔后投石机 224 溅射/发 vs 塔单体 1.9/t,是集团近战波的唯一倍增器;
  seed 1003/1006/1012/1013 翻胜,多个败局存活延长。新胜种子破区：
  1003/1006/1012/1013（六常败种子名单开始瓦解）。
- **EXP_TURTLE 判死（默认关）**：敌 ≥2× 我军时全军收 TC 塔火重叠区——
  军队照样死（13-17 单位的波冲进 TC 区 brawl,站哪都是死）,且与 mangonel
  组合时把增益完全抵消（3/10 vs 单开 5/10）。
- **EXP_WTFIRST 无效（默认关）**：WT 提到 Forging 前——首波 ~1.2-2.2k 到达,
  feudal→WT 研究链来不及完成,改序无收益。
- **第二轮补测（同夜，均未采纳）**：
  - `aoe.expCampRebuild`（交存线重建）：零触发——**矿场其实没被拆**。真螺旋 =
    村民 3 号阵亡 → W=0 补不起 4 号 → 采石工永缺 → 塔 0（配额 feudal 恰好
    1木2金1石填满 4 人，缺一人石就断）。
  - `aoe.expEcoKill`（Bow Saw 30→15 + t8 排队 1→2）：7/20 净负——Bow Saw 提前
    扰动脆弱的开局经济，seed 1006 胜翻负。木瓶颈是真的，但解法不是更早
    研 Bow Saw。
  - **残局解剖（下阶段入口）**：败局末态 G 反而囤积（78-157）而 W=0、
    mil=0、towers 0-1——4 村民硬上限 vs ×8 经济的永久围城下，木是唯一
    卡点且所有解法（加伐木工=配额禁区、省军费=v19/v20 旧证伪）都撞墙。
    全图 40% 已高于"规则式天花板 20-33%"的旧口径；再往上大概率要靠
    B.5 点名的"逐种子 build-order 离线搜索"（把开局序常数旋钮化爬山）。
- 批测工具：ailoop `EXTRA_D` 环境变量透传任意 -D 旋钮（曾因错传环境变量
  空转两批——Java 读的是 System.getProperty,不是 shell 环境变量）。
- 迷雾诚实模式的同源攻坚（下一阶段）待做:mangonel 链是迷雾无关的,预期
  直接复用;诚实模式额外欠账=探图速率(见第六批消融)。

- **逐种子 build-order 离线搜索（B.5 兑现，2026-09-06 第 42 夜，aiK.* 旋钮批）**：
  把 build-order 常数旋钮化（`aoe.aiK.*`，约 20 个：军事生产门/近战门/塔门/
  塔上限/铁匠铺门/t8 门与排队/Bow Saw/StoneMining/村民配额全家——默认值=原行为，
  已验证 19 种子 [ai] 行逐字节一致），对 11 颗败局种子做 14 组单旋钮 + 3 组组合
  筛选。**结论：零救活，当前 8/20 构型是 build-order 常数空间的局部最优**——
  - 降塔门(18/4/12)、降铁匠铺门(22/12)、降 t8 门(20/20)、qBootW=3：败局**死得更早**
    （提前建军事建筑=抽干塔/经济的启动料，波 1 窗口更脆）。
  - t8q=2（expEcoKill 捆绑件的单独分解）：无翻转，1002/1007 存活延长 ~10%
    （登记：捆绑件净负的主因确系 Bow Saw 15 不是排队 2，但排队 2 单独也无益）。
  - milVills=2 / meleeW=10 / bowsawW=20 / smTowerUnder=0 / towerCap=6 / qFeudW 类：
    与基线逐 tick 一致或纯扰动（村民数 <3 的停产螺旋在这些图上不是死因）。
  - 最优组合 C1（t8q=2+qFeudG=1）：屏幕相位翻 1014 但 phase0 不可复现，全局验收
    **5/20（基线 8/20，净 -3）**——配额扰动会翻掉相位边际的存量胜局（1018 型）。
  - **判别法教训（工具）**：ailoop 的 phase pin 按局序 i 取值——不同 `-n/-x` 命令
    形状下同种子相位不同，**跨命令形状比较无效**；筛选必须同形状基线对齐。
    另：EXTRA_D 多参数曾因 ailoop 带引号展开并成单个 argv → 属性值垃圾 →
    ExceptionInInitializerError → AI 裸奔假 LOSS（三屏数据作废后才暴露，已修）。
  - 方向判死：**败局签名（W=0、G 囤积、mil 被歼）对开局序常数不敏感**——
    12 颗败局的 ~2.5-2.9k 首波窗口在 ±30% 死亡时间扰动下无一翻胜。再往上
    大概率不在 build-order 空间，而在战斗微操（承伤结构）或信息（探图速率）。

### 战斗微操离线搜索（2026-09-06 第 43 夜，exm.* 旋钮批：EXM_FOCUS 转正候选）

基线 8/20 同第 42 夜。只动波 1 防御期战斗微操，旋钮前缀 `aoe.exm.*`（契约同
aiK.*：未设置=逐字节当前行为，已验证两带全 20 局 `[ai]` 流一致；**裸 diff
game.log 会被菜单导航墙钟噪声全打红，必须 grep 出 `[ai]`/`[result]` 行再比**）。

- **EXM_FOCUS 防御集火（`aoe.exm.focus=1` + `aoe.exm.focusD2=64`，转正候选，
  两带 10/20 = 基线 +2，8 存量胜一局不丢，样本外 1020+ 带 1/10→2/10 再 +1）**：
  DEFEND 期未接敌军事单位显式指向同一目标格（可见敌军中 HP 最低、平手取最近、
  距防御锚点 ≤8 格门），替代各自站桩自动索敌——战役 #6 v8 的 doctrine 首次
  移植到随机图。杀伤率遥测实锤：seed 1006 敌 2.5k 存活 14→10、1002 +3 杀；
  存量胜局普遍提速（1012 26246→10246、1006 4587→4057）。retask 禁律（目标格
  未变不重写 slot[2]、攻击态单位不重定向）与 48t 重发节奏照旧。
  **门宽是几何敏感参数**：≤6 格 7/20（丢 1012/1013——候选进出半径=目标抖动）、
  8 格 10/20（恰覆盖紧凑塔环 3/5/7/9/11 的主力段）、10 格 7/20（梯度反转，
  追出圈=v2 教训重演）。转正=focus 默认 1+focusD2 默认 64（各一行）。
- EXM_DANCE 闪避上限自适应判死（`aoe.exm.dance=1|2`）：被围（敌≥2×我）全员
  闪避=输出归零败局死得更快（1002 2621→2561）；只 1 人闪避在 1018 翻胜但那是
  相位边际种子，弱证据不采纳（两带 8/20 持平）。
- EXM_REPCAP 修塔扩编判死（`aoe.exm.repcap=3|4`）：两带 8/20 持平，repcap=4
  在 1019 拖出**新形态 STALL**（修理容量上升→村民长钉塔环→战局拉锯双活）。
- EXM_HEAL 回血阈值判死（`aoe.exm.heal=1|2|3`）：三方向全 4/10——<130 撤=
  防御 DPS 降→敌 peak 涨到 21 打不出 CRUSHED（1006 实锤），>200 归同样破
  CRUSHED 节奏。**现值 100/220 是局部最优**，别再动。
- 微操空间的边际判定：集火能提高杀伤率、加速存量胜局、翻活跃相位边际种子，
  但 1002/1009 型**零塔石贫图的败局微操救不动**（towers=0 时承伤结构不存在）。
  微操方向还有剩余空间，但下一个杠杆更可能在"诚实模式探图速率"或逐种子
  相位敏感性上。

### 引擎幽灵队列修复 + 围城应急生产证伪（2026-09-07 第 44 夜，分析提案轮）

分析 sub-agent 五提案的落地轮。基线 10/20（EXM_FOCUS+EXP_MANGONEL 均转正）。

- **引擎幽灵队列泄漏修复（c.java 两处，保留）**：①产出一瞬 spawn 本体 `a()`
  失败（唯一可达条件=单位表满 26；且 `a()` **先 payCost 后帽检**——钱扣了兵
  没出）清整条队列但不递减 `hdr[49]`/`hdr[66+型]`；②`onThingDestroyed` 完工
  建筑带队列被拆不递减（在建 0x40000000 的 byte2 是施工进度流出位、研究中
  0x20000000 的 byte2 bit0 是研究标志 `tryResearch |= 0x10000`，均已排除）。
  幽灵排队永久占 `canTrain` 的"存活+排队<帽"与 pop 的 `[2]+[49]` → 兵种线/
  村民线静默锁死，敌我同病（`tryTrainAiUnit` 同消费面）。注：**canAfford 失败
  不泄漏**（进度停在满值每 tick 重试=生产停摆，队列保留——"产出时 canAfford
  卡"注释语义）。批测定性：两带 20 局**逐种子 tick 级与修复前完全一致**
  （10/20 持平）——该路径在当前 Expert 全图对局中不是决定性变量；修复保留
  （正确性，regress+replaycheck 全绿）。
- EXM_EMGPROD 围城期裸成本应急生产判死（`aoe.exm.emgprod=1`，默认关）：威胁期
  （DEFEND 激活或任一完工塔 HP<220）剑士门 15/25→5/5、t8 25/25→20/20。两带
  **8/20 = -2**：1012/1015 WIN→LOSS 且死得更早（10246→5183、7548→5257）——
  围城期裸木砍给肉盾近战=与塔/房抢同一条 5-20 木的命脉。塔才是防御本体
  （mangonel doctrine），224 溅射下肉盾是 chum；W∈[5,15) 的补兵冲动是负和。
- EXP_HORSECOLLAR 判死（`aoe.expHorsecollar=1`，默认关）：磨坊 tech 6 研究链
  一行（10W/5G）。**开火率天然被压制**：磨坊建造门 S≥15 与塔抢石，10 局仅 1 局
  开火；叠加 emgprod 边际 0（1000 带 +1、1010 带 -1，1018 翻负）。
  **unit-stats.md tech 6 已勘误**：效果=训练速度 [56] +50%（c.java 6343），
  非"木产量[50]+5"数据表误注；磨坊完工本身已 +50%（6233），两级 1024→1536→
  2304，剑士 64t→28.3t/个。
- 微操遥测卫生：DEFEND 行加 `mil=N` 注记——残局 mil=0 的假 focus 行不再误导
  尸检（game3 2758-2998 教训）。

### 石贫图分支探索判死（2026-09-06 第 45 夜，aoe.expStonePoor 旋钮批）

目标：为「零塔石贫图」（1002/1009 型）做检测信号 + 波 1 前主动出击的替代策略
分支。**结论：判死——信号层与策略层双重证伪**，基线维持 10/20。

- **检测信号**（`aoe.expStonePoor=report` 纯遥测档，零行为差；`aoe.spNear` 调
  近距界，默认 14）：扫可采石格（kind3 + 可站邻格 + 未拉黑/蹲守 + 避敌 TC
  10 格；诚实模式限已探索格 + 近距盒 ≥40% 探索覆盖门，两侧同口径），TC
  Chebyshev 14 格内零可采石 → stonePoor 单向 latch（采竭格 c.java:7393 清资源
  位，重扫自然跌出）。**静态石贫无区分力**：t≈0 全图扫描，败种 1002/1005/
  1007/1009/1010 近距石 7-29 格全有（1009 near=29 最富照样败），胜种 1003
  near=4 照胜；早期判贫只命中 1014/1015/1016，其中 1015 是胜局（误判 1/3）。
  **43 夜「零塔石贫图」定性需修正**：这些图开局石够建 3 塔（起始 100 石），
  末态 towers=0/S=0 是围城的果不是因——败种的战中 latch（石被波次蹲守/采竭）
  全在 t=1182-1416，晚于波 1 launch（1.1-1.3k）。
- **V1 SPRUSH 早闪击（=1，判死）**：早期判贫（latch ≤600t）+ mil≥5 +
  t+1500<waveTick → 无集结直扑敌 TC。1010+ 带 4/10：**1015 胜翻负**，
  1014/1016 无翻转。机制：5 民兵接敌掉 1 人即 RETREAT（t≈750，256t 自爆）。
  唯一正信号：e55 压制有效（1014 自然波 1220→2116，推迟 900t）。
- **V2 死战版（=2：撤退阀 4→1 + 回撤冷却 1500→400 + 触发 6 兵，判死）**：
  4/10 更糟——1014 冲军 376t 折 5/6；且**闪击触发敌防御模式 87.5% 反扑不需要
  阈值**（e55=56 即发波，t=826-843，比自然波提前 ~400t 砸到塔未完工+军不在
  家的家）。TIMERUSH 在诚实 T≥150 成立的前提（4.6k 积累真空 + 塔已完工）在
  Expert 全图都不存在。**别再试：Expert 波 1 前的主动出击/换家**。
- **V3 塔链优先重排（=3：判贫图 smith/射箭场让位到塔 3 之后，判死）**：
  5/10 持平但目标种 1014 死得更早（2962→2643）——塔 3 提前到 844（m55
  73→90 达标），但 smith/射箭场被磨坊（15S）抢位后石竭永建不起，弓兵/t8
  杀伤率倍增器全丢；1015 胜保住但变慢（7548→9397）；1016 逐字节不变（其
  塔 3 本就在 smith 前，门不绑定）。**别再试：起始 100 石上重排 smith/塔序**。
- **败因测绘（波 1 后 ~700t 截面，t≈2000）**：胜者 = mil 6-9（val 48-108）
  + towers 3-4 + **射箭场已开**（胜者 9/10 有 archery 且多在 ≤3.4k；败者
  8/10 全程无——例外 1014@1204 仍败、1010A@2231 太晚）；败者 mil 2-8
  （val 14-88）、towers ≤3、敌 peak 11-19 不止血。敌首波构成全种子一致
  （launch 1.1-1.3k、6 兵 val 36-52——×8 经济积累与图无关），分野在**我方
  波 1 后的产能链**：围城中村民死亡 → 木/石断供 → archery/smith 卡死，且卡
  哪种资源逐图不同（1002 木、1005 石差 1、1017 石、1019 木金双断）——系统
  性赤字无单一解，与 42/44 夜（build-order 局部最优/配额禁区）互洽。
- 旋钮全部默认关（未设置 = 逐字节当前行为，两带 20 局 grep [ai]/[result]
  零差异实证）；regress 三连 + replaycheck 全绿。检测器 report 档保留——
  未来任何「按图分策略」的现成入口。


战略一句话：**塔防吸收敌方 all-in → 集结后反击拆敌 TC**（拆 TC 即胜）。拼经济
必输（敌 3.07×/8× 采集 vs 我方 4 村民硬上限 + pop 25），塔不占人口且完工计入
hdr[55] 军值（威慑敌 all-in 判定），是唯一的免费战力。

每 8 tick 决策一次，模块顺序（带 ★ 为第六批诚实模式新增）：

0. ★**信息层**：可见性过滤（evis：敌单位所在格已探索才进任何计数）；敌建筑
   扫描记忆敌 TC（`enemyTcMem`）与最近目击方向（`enemyHint`）；首波接触来向
   （`waveOrigin` = 新接触时离我 TC 最远的可见敌兵）。全图模式全部旁路。
0b. ★**侦察**：scout0（t5 优先，否则首个军事单位）走螺旋路点（TC 外扩正方形环，
   弧距 ≤3=视野无缝）；有接触来向改走射线（延长线反推+横向锯齿）；milCount≥6
   加 scout1（错开半圈）；威胁中 milCount≥3 保持侦察；卡死 300t 跳路点。
   敌 TC 未知时塔位降级为绕 TC 塔环（有来向则环序朝来向旋转），僵持 15k 后
   全军 HUNT 扫荡开图（路点不可达 1500t 无进展跳过）。
1. **态势扫描**：我方村民按 action/目标格归工种（木/金/石），军事计数量与价值
   （Σ(攻+甲)）；敌方计**可见**军值/军数（估值用我方同兵种数值），可见敌塔折
   "防御军值"。波次预测器仅全图模式启用（读 ehdr[55] 的只读遥测）。
2. **弹窗自关**：ss==2 时 `onKeyPress(-7)` 后返回（z=62/z=70 信息弹窗冻结世界，
   headless 无人关窗 = 永久 STALL，seed 1010 实测冻 27 万 tick）。
3. **军事**：
   - 敌兵进我方 TC 10 格（DEFEND_D2=100）→ 近战群令压上；弓兵/投石机停在
     入侵者朝防御锚点 3 格处开火（远程不送近战圈）；敌 t8 在 20 格内时远程优先点杀
     （投石机远程拆塔，近战无解——agent-operations §5.1/r29）。
     **塔援护（仅 Expert）**：敌兵贴近任一完工塔 6 格内同样触发 DEFEND——
     Expert ×8 波次会逐个拔掉外圈孤塔（v15 复盘：5 塔被白拆 4 座军队在旁观）；
     Medium 上同机制反而把军队拉离 TC 塔圈吃败仗（v16 实测 6/10），故按
     `aiGatherMultiplier >= 1024` 难度门控。
     **Expert 波 1 接战微操（v35 四件套，全在 expert 门内）**：① 环形拦截——
     近战钉在锚点朝敌 3 格塔火重叠区等接敌，不冲入侵者（冲锋=出圈被围殴）；
     ② 集火闪避——被敌攻击态锁定（敌 slot[5]==我格）的单位每决策横移一格
     （攻击按格结算，离格=敌装填清零重追），落点塔圈内优先，人数上限
     milCount/2；③ 投石机猎手——敌 t8 贴近塔/TC 10 格派 ≤2 快腿近战直取
     （塔对射射不过 t8），每决策重投；④ DEFEND 逐单位下令——攻击态/回血中/
     猎手/侦察兵单位不被 48t 群令重发打断装填。遥测：摘要行 `dodges=/hunts=`。
   - 残血（HP<100）回撤 TC 背敌侧站桩回血（0.5 HP/tick），HP>220 归队——
     消耗战里保老兵 = 省替换金，对 3.07× 产量的敌人是刚需（v11 关键胜率来源）。
   - 反击触发（需家门清净 + 敌 TC 已知）：敌主力被歼（可见峰值 ≥5 且现存 ≤1/3）
     / 碾压（军值 ≥130 且 ≥ 敌防御军值 4/3）/ 僵持兜底（t25000 后）/ **贴脸图闪击**
     （双方 TC ≤14 格：消耗战必输，兵力不劣直接上）/ ★**金竭突击 GOLDSTARVE**
     （金不可派且存量不够再训——与其坐耗等死不如搏拆 TC）。**Expert 反击门槛
     抬高**：≥10 兵且军值 ≥ 敌塔计入后的防御军值（7 兵反击=把家底送进敌塔环）。
     进攻中小股（≤3）反扑不撤，大股撤；1500 tick 无进展判定停滞撤回重整。
   - **进攻两阶段（v14）**：先全军集结到敌 TC 朝我 7 格（警戒圈 6 格外沿，
     顺带触发敌防御模式反扑空巢），到齐 2/3 或超时 700 tick 再一起扑敌 TC——
     兵种移速差 4 倍（投石机 256 vs 剑士 1024），群令直发=添油送死
     （v13 败局 2001/2006 复盘实锤）。
   - 互瞪僵局诱敌：t15000 后久无接触且敌蹲家 ≥10 兵 → 派最便宜的兵直扑敌 TC
     触发其防御模式反扑，喂给我方塔阵（§5.3 调虎离山；驻防群令会召回诱饵，
     靠每决策重投续上）。诚实模式需敌 TC 已知。
4. **村民**：闲置（action=0 且已到目标格；采集→交存→返矿全自动，§10 定论）
   与卡死（行军 300 tick 未动 → 目标拉黑 3000 tick）重派；配额 boot 期 2木1金，
   封建后 1木2金（塔 <5 座时压 1 人采石），开战后（敌亮过 6+ 兵）1木3金
   （v41 起铁匠铺未建成也保 1 石工——塔满后石工归零会让 smith 永远没石）。
   在岗配额漂移由"每次决策最多换 1 人"的主动再平衡修正（含 v15 石断供补救：
   需石而石工为 0 时放宽抽调门槛）。敌兵贴脸 7 格 → 村民撤命，**v38 起 Expert、
   第六批起诚实模式全难度撤到"最近未蹲守同种资源格"直接复工**（逃命不失业；
   找不到才退 TC 另一侧）。
   ★**发现探针**：木或金整类无可派格时，前两个闲村民改派近环探路；全员在岗
   时抽专职探员走全环（Expert 除外——波 1 前抽工代价更大）。单向翻转不振荡。
   资源点用自扫 findResource（引擎 findNearbyResource 缓存双方共享，会把敌村民
   引到我方资源点互殴）；**findResource 有战区概念（v14）：距敌 TC <10 格的
   资源格不派工、不建营地**（无候选才回退不过滤）——v13 seed 2004 复盘：
   最近金矿在敌 TC 3 格处，矿工逃命/重派死循环金收入归零，矿场原地被拆 6 次。
   **v38/v45 起 Expert、第六批起诚实模式全难度追加蹲守标记**：敌兵 8 格内的
   资源格打 camped 标记（滚动窗 1500t=敌走后才解禁，防两矿来回跳振荡环）；
   转移只派"比我 TC 离敌更远"的走廊外资源格（Expert 限定）。
   **纪律：村民配额/再平衡全是 +1 缓冲的阻尼收敛，不许做"严格收敛/急救抽调"——
   会与逃命链互激成震荡环（v18/v19/v22 三连败实锤，v18 单局 36 次 rebalance）。
   连条件式目标值转移也连败（v37/v39）——配额类改动累计五连败，视为禁区。**
5. **科技**：封建（兵营前置）→ Forging / Watch Tower / Gold Mining /
   Double-Bit Axe / Bow Saw(W≥30 才研) / Scale Mail（金 ≥25 才研）/
   Stone Mining（塔未满 5 座时）；城堡时代（磨坊+铁匠 ≥2 且资源富余）→
   Iron Casting / Chain Mail / Guard Tower。
   科技必须尽早就位——GoldMining/WatchTower 直接决定首波窗口的金收入与塔生存
   （v5 推迟到 8k 的实验证明是灾难）。
6. **建筑**（一次一座，放下自动成型 ~32 tick）：房屋1 → 伐木场 → 兵营 →
   塔1 → 采矿场 → 塔2 → 铁匠铺 → 射箭场 → 塔3-5 → 房屋2-4 →
   磨坊 → 马厩 → 攻城工坊。塔位 = 我方 TC → 敌 TC 走廊上 4/6/9/12/16 格阶梯
   （r26 验证的最优汇率防术；近距图钳到走廊 60% 内，别蹭进敌警戒圈）；
   ★敌 TC 未知时降级为绕 TC 塔环、有接触来向则朝来向旋转。
   伐木场/采矿场锚点=已探索资源格，**找不到时条件纯查询跳过（锚点进分支体再算——
   v57 条件内赋值冲掉 anchor 吞掉整链的教训）**。
   **交战中（敌兵 12 格内）只补塔+补房**（v36：塔被拆第一时间原地补，门槛
   22木/6金/16石；房 5 木=最便宜人口+仇恨海绵——人口帽被磨掉=金囤着变不成兵）。
   **Expert 专链**：紧凑塔环 3/5/7/9/11 格 → 塔 2 后立刻补满 4 房 → 第二兵营 →
   塔 3-5（**v47：塔 4-5 只在开战前优先；敌亮过 6+ 兵后让位给射箭场/铁匠铺**）→
   **射箭场（v42 提到铁匠铺前，v56 门槛 30木/12石）** → 铁匠铺（v56 Expert 门槛
   30/22）。金矿被蹲时在 ≥8 格外的备用矿点旁放**第二采矿场**（v40）。注意
   `queueUnitTraining` 只排同类型**第一座**建筑（c.java:6519 记录序扫描），
   第二兵营靠直写队列位（rec[+2]+=65536 + hdr[49]++ + hdr[66+t]++）。
7. **生产**：兵营（剑士/民兵）/射箭场（弓兵）各维持 ≤2 在训，铁匠铺 ≤1 台
   投石机（金 ≥40 才排；**t8 产自铁匠铺不是攻城工坊**——queueUnitTraining
   case 8 → 建筑 6），马厩 ≤2（封建侦察/城堡骑兵自动换形），攻城工坊 ≤1 冲车
   （金 ≥60）。产兵扣款在产出时，排多不亏但占人口名额。
8. **修塔（v44/v46，仅 Expert）**：最残的完工塔/TC（HP<245）派 ≤2 个最近
   满血（HP≥220）村民修理——走到受损建筑格自动进 action 4，~0.5 HP/tick/人
   且免费，1 人恰好抵消 1 剑士磨塔。修理工不参与逃命（否则敌兵贴脸 7 格的
   逃命规则每 8t 把修理工拽走），但 HP<200 即退役回逃命链（挨一刀就放——
   钉在塔环被集火曾单局送 12-13 个村民）。遥测：摘要行 `repairs=`。

确定性：只按 tickCount 节流，无墙钟、无随机数（也不许碰游戏 nextRandomInt）。

## 怎么跑

```bash
# 批量胜率（10 局 Medium，种子 1000 起，-b 开 BFS 寻路，-k 留每局日志；
# 默认即迷雾诚实模式）
tools/ailoop.sh -n 10 -d 2 -a aoe.ai.RuleBasedAi -s 1000 -t 240 -k -b
# 全图对照（回退 v56 等价路径）
tools/ailoop.sh -n 10 -d 3 -a aoe.ai.RuleBasedAi -s 1000 -t 240 -k -b -f
# 消融诊断（资源全图+敌情诚实 / 敌TC全图+其余诚实）
AOE_AIFOG=res tools/ailoop.sh -n 10 -d 3 -a aoe.ai.RuleBasedAi -s 1000 -t 240 -k -b
AOE_AIFOG=tc  tools/ailoop.sh -n 10 -d 3 -a aoe.ai.RuleBasedAi -s 1000 -t 240 -k -b
# 窗口观战
./run.sh    # 或任意方式启动后加 -Daoe.playerAi=aoe.ai.RuleBasedAi（-Daoe.tickms=40 加速）
```

⚠️ **改了代码再跑批，编译必须独立成行并亲眼确认 BUILD SUCCESSFUL**——
`./gradlew classes 2>&1 | tail -1 && tools/ailoop.sh ...` 的管道会把 gradle 退出码
吞成 tail 的 0，编译挂了照样跑批（=拿旧类文件空转）。第五批曾因此空转八批
（2026-09-04 事故，见迭代笔记）。判别：成功构建尾行是 configuration-cache 提示，
失败是 "1 actionable task: 1 executed"。

日志：`[ai]` 前缀（assign/rebalance/probe/build/research/DEFEND/ATTACK（带触发原因
CRUSHED/OVERWHELM/DESPERATE/CLOSERUSH/GOLDSTARVE）/HUNT/CONTACT/SCOUT/
RETREAT/ABORTED/STALLED/BAIT/STUCK + 每 500 tick 态势摘要；诚实模式摘要含
enemyVis/eb/etc=敌TC坐标或?）；诊断配合 FIFO `aistate`（`tools/aoectl aistate`）。

## 已知边界

- seed 1004 型退化图（无可达金矿 + 敌 TC 被塔围死）：僵持，可接受（三难度同图同症）。
- 高方差近距 rush 种子：Medium 2000+ 区间仍有个位败场（最终复测 2004/2008）——
  敌 3.5-6k 到家时金收入上限是硬约束。
- Expert（×8 采集 + 每 tick 出兵 + 免费资源滴=敌村民数/1000t）：全图 40% 是规则式
  当前上沿；诚实模式 1-2/10——缺口在波 1 前的探索速率（单位视野 3×3 焊死信息
  天花板），不在战斗微操（第四批已证伪）也不在围城经济（第五批已定性）。
- **双方僵尸局**（诚实模式新增边界，2026-09-06 已修）：村民全灭 + 木=0 + 塔在场 → 不满足失败条件、
  产不出村民（训练要木），敌也磨不动塔 → 空转到墙钟 STALL（Medium 1006 两批
  连续实锤；相位 pin 下复现相位=devPhase 14，v62/v64 两批早于 pin 故原相位
  不可考）。动作空间内无解（无市场无变卖），批测侧出口 = 投降门新增「村民
  死锁」分支（vills==0 && 木<5 && 无在训村民 → 视同三系收入死断，500t 后
  concede；日志带 `(villager deadlock)` 标记）。
- 同一种子跨跑结果会翻（菜单导航墙钟相位）——诚实模式马拉松局放大该噪声
  （v62/v64 行为等价的 Medium 同批种子 8/10 vs 4/10）：只信多批合并带。

## EnemyAi（反串 player 1，2026-09-07）

**定位（用户拍板 2026-09-07）：默认敌方 AI 永远是原版引擎行为（tickAi
生产脚本），增强版 AI 仅为可选实验开关**——不设 `-Daoe.enemyAi` 时游戏与
原版逐字节一致，需要时（对局实验/标定/自对弈研究）显式挂载使用。

`-Daoe.enemyAi=aoe.ai.RuleBasedAi` 让 RuleBasedAi 反串敌方（player 1），取代引擎
原版敌 AI（tickAi 生产脚本），作为可选的强化对手。次要场景：同时挂
`-Daoe.playerAi=aoe.ai.RuleBasedAi` 自对弈（平衡观察）。

**挂载**：c.java onPaint 帧首与 playerAi 并列的 hook（同契约：反射装载失败/
tick 异常打 `[ai]` 并禁用，不影响游戏；装载优先取 `(int side)` 构造传 1，无则
回退无参）。装载成功且 gameMode==0（随机图）时 `enemyAiActive=true`，模拟段
跳过 tickAi——敌方不再被旧脚本驱动（tickAi 的威胁扫描/免费资源滴/建筑/出兵/
村民改派/stance 推进全部只服务 player 1，整体可跳）。战役/教学（gameMode
16/32）aiEnabled=false 静态守军是有意设计，enemyAi 打一行日志后忽略。

**side 参数化**：无参构造 = side 0（现状）；`RuleBasedAi(1)` = 反串。原 player-0
硬编码统一走 `this.side/this.foe`（hdr/slots/buildingTable 下标 + selectUnits/
orderMove/queueUnitTraining/tryResearch/canAfford/a 首参，共 ~70 处替换）。
side 1 默认强制全图（fogHonest=false）：引擎迷雾是 player-0 单层（mapTiles
0x8000 只被 player 0 单位揭开），player 1 没有自有雾层可诚实——侦察/探针/
敌 TC 记忆模块全挂在 fogHonest 门上自然旁路；**arena 模式（下节）下 side 1
用私有 exploredBitmap 解锁诚实模式**。side 1 非 arena 永不打印
`[result]`/System.exit（那是 player-0 视角批测契约，僵尸投降门整个跳过；敌方
胜负由引擎 z=98 结算代打）；arena 下投降门开启（concede side=1 → exitOnResult
时 `[result] WIN`+exit）。静态可变状态：无（2026-09-07 第 0 轮起旋钮也全部
改为构造期按侧解析的实例字段），两实例同 JVM 共存安全。

**引擎不对称发现（side 1 适配的根因，都实读 c.java 验证）**：
- ~~**研究完成效果只在 tickBuildings 的 i==0 分支生效**~~ **【2026-09-07 已对称化，
  见下「科技对称化」节】**：techFlags 曾是全局单份=player 0 科技态，player 1
  研究完成静默清零、升时代永不落地。现有 `enemyTechFlags` 平行数组 +
  tickBuildings i==1 平行效果块，side 1 科技模块已解禁。
- **出兵形态键**（民兵↔剑士、侦察↔骑兵）= `spawnAgeKey(p)`：默认路径
  player 1 仍键 player 0 时代（`playerUnitHeaders[0][0]`，原作语义，
  convertUnitType 全局迁移双方队列计数与之配套——引擎敌 AI 搭便车，行为
  逐字节不变）；enemyAi 接管时 side 1 键自己的时代 `hdr[1][0]`。RuleBasedAi
  的 meleeType 同键自己的 hdr[0]，队产一致。
- 弓箭手/投石机/冲车的 spawn（case 7/6/2）无时代门——side 1 不升时代也能出
  全兵种；塔完工注册（case 12）按 player 参数化，天然对称。
- **起始资源不对称**：Easy 起 player 1 只有 50/15/15（player 0 恒 200/100/100）
  ——boot 配额 2木1金 无石工，建完兵营 S=5 永远攒不够封建 15S。→ side 1 补丁：
  S<15 且有矿场时 boot 期保 1 石工（side 0 起始 100 石永不进此分支）。
- 采集乘数（aiGatherMultiplier ×2/×3.07/×8）在交存结算处按 player 1 生效，
  side 1 RB 照吃难度加成；免费资源滴在 tickAi 里，随抑制一起停用。

**验证**（seed 1000 系，-b BFS，turbo headless）：
- enemyAi vs 空转玩家（Easy）：`[ai] side=1` 正常发育（射箭场+5 塔+9 兵），
  t=25008 DESPERATE 总攻拆空转玩家 TC → 引擎 `[result] LOSS ticks=25552`。
- 自对弈（Medium 5 局，两侧同 AI）：见下方批测数字。
- 默认路径零行为差：不设 enemyAi 时 regress 三连 PASS、replaycheck 一致、
  ailoop 5 局与改动前基线逐种子同结果、`[ai]`/`[result]` 流仅 boot 行格式
  差异（加 side= 前缀）。

**已知限制**：民兵帽 4 卡早期军值，进攻多靠 DESPERATE 兜底触发（对
空转玩家）或正常 CRUSHED/OVERWHELM（对真玩家）；自对弈时两侧共享
aiGatherMultiplier/aiAttackThreshold 难度旋钮语义（值按"敌方难度"解读，对
side-1 实例是自身加成）。

### 科技对称化（2026-09-07，真对称化——非 boost 旋钮）

player 1 的研究/升时代真正生效。架构 = **平行数组**：player 0 的全部
techFlags 使用点一律不动（默认路径逐字节不变的语义基础）。

- `enemyTechFlags`（c.java，与 techFlags 同从 res#127 装载初值）= player 1
  的科技旗标；**player 1 的时代 = `playerUnitHeaders[1][0]`**（引擎从不给它
  +1，现由研究完成块推进）。
- **研究完成块 per-player 化**（tickBuildings）：i==1 走
  `applyEnemyResearchEffects`——逐 case 镜像 i==0 的模拟效果（写
  playerUnitHeaders[1]、置 enemyTechFlags 位、hdr[1][0] 升时代），跳过全部
  UI（简报/横幅/requestStateSwitch 全是 player-0 专属）。
- **convertUnitType 是全局的**（转双方在场单位 + 迁移双方 hdr[57+x]/[66+x]
  队列计数）→ 新增 per-player 变体 `convertUnitTypeForPlayer`。side 1 升时代
  只转自己的兵；**enemyAi 局内 player 0 升时代也改走 per-player(0)**（否则
  会错转 side 1 在场民兵并把其队列计数迁走，与 side 1 自己的形态键打架）。
  默认路径（!enemyAiActive）player 0 升时代仍走原全局调用，逐字节不变。
- **tryResearch 前置检查按 player 分派**（techFlags vs enemyTechFlags）。
- **塔升级 quirk 处置**：i==0 块研塔科技连 `hdr[1][12]`（塔程）一起写的
  搭便车**保留不动**（默认路径行为底线；enemyAi 局内 player 0 研塔科技仍会
  顶掉 side 1 自己的塔程——已知残留）；side-1 塔科技只写 hdr[1][45/46/47/12]，
  不动 hdr[0]。
- **塔贴图 tier（c.java `b(Graphics...)`）owner 感知**：P1 塔皮改看
  enemyTechFlags——行为变化（已裁决）：原实现双方塔皮都吃 player 0 塔科技，
  默认路径下 P1 塔现恒 tier 0 贴图。纯渲染索引（n6 只喂 drawTileSprite/血条
  几何），不进模拟态，regress/replaycheck 不可见。
- **残留 quirk（未动，注意）**：塔放置成本（`payCost(n,1,12+tier)`）仍键全局
  techFlags——side 1 研塔科技后新塔仍按 player 0 的 tier 计价；战役脚本
  条件 6/动作 9 保持绑 player 0 techFlags（enemyAi 只在 gameMode==0 生效）。
- **SaveState v5**：enemyTechFlags 进快照（capture 末尾/apply version>=5 门），
  旧档（v2-v4）可读=保持 res#127 初值（旧版行为）。

**对称化验证（2026-09-07）**：regress 三连 PASS + replaycheck 一致 + devBoot
双跑对拍一致（v5 档）；合成 v4 旧档加载正常（enemyTechFlags 落初值）。无
enemyAi 基线（`-n 5 -d 2 -s 1000 -b`）与 HEAD 逐种子 ticks + `[ai]`/`[result]`
流逐字节一致（1000=15960W/1001=2263L/1002=3821W/1003=7241W/1005=5012W）——
引擎敌 AI 路径零扰动。功能：FIFO 局实证 side=1 FEUDAL t=1161 落地
（age=1、在场民兵转剑士、后续 spawn 全剑士、GoldMining/WatchTower 等 5+ 科技
落地）。⚠️ 本机（Linux）注意：tools/bootcheck.sh 因 stopat 后 headless JVM
自杀（无窗口线程吊命，dev-mouse 是 daemon）在 echo state 吃 SIGPIPE exit=141
——HEAD 同症，平台预存问题；对拍可用同一流程把载体从 aoe.Main 换成
aoe.DevHarness（自带看门狗线程吊住 JVM）手工完成。

### 标定矩阵（2026-09-07，ailoop `-e` 透传，尺子 = side 0 诚实模式 RB）

形状统一：`-n 10 -d N -a aoe.ai.RuleBasedAi -e aoe.ai.RuleBasedAi -s 1000 -t 240 -k -b`
（side 0 诚实模式，side 1 强制全图）。对照 = 同形状无 `-e` 历史基线。

**科技对称化前（第 46 夜，side 1 无科技）**：Easy 8/10、Medium 8/10（另一批 4/10，
合并 12/20）、Expert 1/10——等效难度 ≈ 同名引擎难度。

**科技对称化后（本轮，side 1 科技真生效）**：

| 敌方参数 | side 0 胜（对称化后） | side 0 胜（对称化前） | 解读 |
|---|---|---|---|
| Easy（×2，50/15/15，T=50） | **6/10** | 8/10 | 起始 50/15/15 攒 15 石慢，封建 ~1.1k 才落地，科技红利迟到 |
| Medium（×3.07，50/50/50，T=60） | **0/10** | 8/10 | 50/50/50 开局兵营一成即刻封建（~200t），Forging/ScaleMail 全在首波窗口前落地——科技平价 + ×3.07 经济碾压 |
| Expert（×8，20/20/20，T=100） | **0/10** | 1/10 | 原已贴地，科技补齐后 10 局中位 3436t 速败 |

结论：side 1 科技是真实的强度维度——对称化把 enemyAi 从「同名引擎难度」抬到
「显著难于同名引擎难度」（引擎敌 AI 永远无科技，本来就是它的隐性弱点）。
Easy 降幅温和（-2/10），Medium 出现悬崖（-8/10）——50/50/50 开局让科技链
在经济曲线上免费。零 `[ai] ... tick exception`；每局 side=1 研究落地 7-12 条。
不确定度：每档单批 n=10；Medium 0/10 与 8/10 的差远超批间噪声（±1-2 局）。

### 对称化旋钮（全部默认关 = 零行为差，c.java 注释段自带语义）

| 旋钮 | 语义 | 注入点 |
|---|---|---|
| `-Daoe.fairStart=1` | 随机图敌方起始资源 hdr[1][5..7] 拉平为 player 0 的 200/100/100 | setupMissionEnv 难度 switch 后（仅 gameMode==0） |
| `-Daoe.fairGather=1` | player 1 交存结算不吃 aiGatherMultiplier（按 256=1× 计） | onUnitArrived 交存 case 256 的 n==1 分支 |
| `-Daoe.enemyDrip=N` | enemyAiActive 时每 N tick 给 hdr[1][5..7] 各加 hdr[1][57]（复刻 tickAi 免费资源滴；tickCount 节流，无墙钟） | tickEnemyAi 帧首 hook |

**对称自对弈（Medium 参数 + fairStart + fairGather，同形状 10 局）**：
side 0 胜 **7/10**（无僵持；对照 = 无 fair 的 Medium 标定批 8/10）。ticks 均
23718 vs 无 fair 批 16159——fairStart 扶敌开局（50/50/50→200/100/100）+
fairGather 削敌中盘（×3.07→×1），两力反向净值 ≈ 中和，胜率变化在批间噪声内。
残差不对称 = side 0 科技生效 vs side 1 全图信息（side 0 背着诚实雾仍赢七成，
即「科技生效」净值 > 「全图信息」净值；n=10 单批 70% 的 95% CI ≈ ±28pp，
与 50% 对称点统计上不显著）。旋钮生效实证：fair 批 game3 日志 side 1 首建筑后
res=195/100/100（开局 200/100/100），无 fair 同种子同相位为 res=45/50/50。
旋钮默认关零行为差实证：加旋钮后重跑的 Medium 标定批与加旋钮前半批逐种子
ticks 完全一致（七局同数），regress 三连 PASS + replaycheck 一致。

### 自对弈竞技场（-Daoe.arena=1，2026-09-07 第 0 轮基建 + 第 1 轮 side 偏差中和）

规格/评估协议/联赛表 = `docs/research/selfplay-arena.md`（唯一权威）。arena 主
开关捆绑：fairStart+fairGather 隐含开启；帧首两侧 AI 调用序按 tickCount&1 交替
（c.java onPaint）；模拟段五循环玩家处理序同奇偶交替（`arenaSimAlt`，第 1 轮）；
Easy/Medium side-1 镜像补出生侦察兵（`arenaSymSpawn`，第 1 轮——原版只白送
P0，遥测实证 t=0 s0 u=3/mv=9 vs s1 u=2/mv=4，单独中和掉 side 偏差 ~15pp）；
每 500t `[arena]` 对称指标遥测（side 偏差诊断用，分叉时点判据：接触前分叉=
开局/地图级，接触后=交互级）；side-1 私有迷雾诚实模式（见下）；side-1 arena
concede；arena+exitOnResult 下 tickCount>50000 判 `[result] DRAW`+exit（隐藏
覆盖 `aoe.arenaDrawTick`，默认勿改）。默认关 = 零行为差（验证：regress 三连 +
replaycheck + 无 enemyAi n5 基线逐种子 ticks 与 [ai]/[result] 流全一致）。

**side 偏差账本（第 1 轮）**：G0 镜像基线 80%（+30pp）→ +arenaSymSpawn 65%
→ +arenaSimAlt 55%/35%/50%（三 10-种子集），合并 60 局 46.7% 入噪声带。
两个偏差源全在引擎原版代码里（出生侦察兵只送 P0；tick 内恒 P0 先行动），
均在 arena 门内中和，默认路径逐字节不变。残差为图运/相位噪声（同图不同
相位可翻胜方）。

**旋钮按侧覆盖**：RuleBasedAi 全部自消费旋钮（aiK.*/exm.*/exp*/spNear/aiFog）
从 static final 改为构造期按 side 解析的实例字段——先查 `aoe.<name>.p<side>`
再回落 base 名，不设 .pN 时与旧语义逐字节一致（批测日志 diff 实证：base 名=
默认值零差异；`.p1=report` 纯遥测只让 side-1 实例打 STONEPOOR 行，side 0 的
[ai] 流不动）。引擎侧旋钮（bfsPath 等）不按侧。ailoop `-m name=value` 消费
此机制做镜像配对。

**side-1 私有迷雾**：引擎雾层是 player-0 单层（mapTiles 0x8000），side 1 在
arena 下改用 AI 实例自维护的 `exploredBitmap`（boolean[4096]）：每 tick 按
自己单位 3×3 + 完工建筑圆形半径 3（塔 6）揭格，镜像引擎
revealFogAroundUnit/void_a 语义（引擎建筑揭雾是 Bresenham 圆且逐 tick 轮流扫
一座，这里用欧氏圆逼近且每 tick 全扫——探索永久累积，差异仅完工首 tick 的
时序粒度）。全部迷雾读点（敌单位 evis/敌建筑可见性/敌 TC 记忆/findResource/
findSecondGold/stoneScan）统一走 `isExplored()` helper：side 0 查 mapTiles
（一字未改），side 1 查 bitmap。**已知限制**：bitmap 是实例瞬态不进存档
（enemyTcMem 等 AI 记忆同例），读档后从空重建；批测一局一 JVM 无读档路径。
冒烟实证（arenaDrawTick=3000 缩短局）：side=1 boot 行
`fogHonest=true (private fog: exploredBitmap)`；全图遥测 `e55=` 行从 5→0；
两侧探针行 t=0 起交错。

**终局规则**：side-1 僵尸投降门在 arena 下开启（同一套收入死断/村民死锁推导），
触发打 `[ai] concede side=1: ...`；exitOnResult 批测路径再打
`[result] WIN ticks=N`（side 1 认输 = player 0 胜）+ exit，非批测只打日志
不 exit（终局仍走引擎 z=98）。DRAW 挂在 onPaint 帧首，只按 tickCount。

**镜像批工具**：`tools/ailoop.sh -m name=value`（candidate 旋钮，`-m -`=无
candidate 纯镜像基线）——每种子跑两局（candidate 在 p0/p1 各一，同相位
pin），自动带 arena=1、双侧默认 RuleBasedAi；CSV 加 cand 列、result 支持
DRAW；summary 增 candidate 合并胜场（DRAW 计半）+ 逐侧拆分 + side0 得分
（side 偏差，对称镜像应≈50%）。--selftest 覆盖 DRAW/镜像统计。

