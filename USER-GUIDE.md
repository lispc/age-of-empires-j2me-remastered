# 帝国时代 II（J2ME 复刻版）玩家使用手册

2005 年手机版《帝国时代 II》的 macOS 桌面复刻。原版玩法完整保留，
键盘 + 鼠标双操作，支持快速存档/读档。

## 启动

```sh
./run.sh                    # 宽屏视野（逻辑 720x320，约 3 倍于原版的战场视野）
AOE_WIDTH=240 ./run.sh      # 原版竖屏视野（240x320）
AOE_WIDTH=480 ./run.sh      # 折中宽度
```

需要 JDK 17（`brew install openjdk@17`）。退出直接关闭窗口。
窗口大小 = 逻辑分辨率 × 自动倍数（按屏幕可用区域取最大整数倍，上限 3；
`-Daoe.scale=N` 可手动指定）。720 宽在常见 Mac 上是约 1440x640 的横窗。

## 键盘操作

| 按键 | 作用 |
|---|---|
| 方向键 / `W A S D` | 移动选择光标（镜头跟随光标） |
| `Q E Z C` | 斜向移动（左上 / 右上 / 左下 / 右下） |
| `Enter` / `Space` / `X` | 确认：选中光标下的单位、执行指令 |
| `F1` | 确认/继续（推进剧情对话框） |
| `F2` / `Esc` | 取消/返回 |
| `0` | **开关全图视图（小地图）**，详见下文 |
| `1`–`9` | 原版备用方向/动作键（斜向移动等） |
| `F5` | **快速存档** |
| `F9` | **快速读档** |

## 鼠标操作

| 操作 | 作用 |
|---|---|
| 移动 | 高亮所指地块（镜头不会乱动） |
| 左键单击 | 选中单位（点单位脚下）；点空地=光标直达并就地确认 |
| 左键拖动 | 框选一组己方单位（拉出矩形框后松开） |
| 右键单击 | 有选中单位时：**全体移动到鼠标所指格**；未选中时=取消选择 |
| 贴住窗口边缘 | 镜头向该方向持续平移（把鼠标移回中间即停） |

## 小地图（全图视图）

按 `0` 进入全图视图，再按 `0`（或 `Esc`）返回。视图里：

- **黑色** = 还没探索的区域（战争迷雾）。刚进关几乎全黑是正常的——
  派单位走出去探索，地图会一格一格亮起来；
- **绿色/蓝色** = 已探索的地形（草地/水面等）；
- **白色四角框** = 当前镜头视野的位置；
- 亮点 = 单位和建筑。

全图上可用方向键 / `WASD` / 数字键平移查看（要按住约半秒才开始移动，
原版就是这样的连发节奏）。再按一次 `0` 或 `Esc` 返回世界画面。

## 存档与读档

| 操作 | 说明 |
|---|---|
| `F5` | 快速存档到 `quick.aoesave`，屏幕中央显示 "Saved" |
| `F9` | 读回 `quick.aoesave`，显示 "Loaded" |
| 自动checkpoint | 每关开始约 2 秒后自动存一份 `auto.aoesave` |

- 存档目录：`~/Library/Application Support/AoeJ2ME/saves/`
- 注意：读档限**同一个任务内**（游戏会校验任务身份，跨任务的档会拒绝
  读取并提示）。"在主菜单继续上次任务"计划在后续版本提供。
- 存档是整局快照：单位位置、地图探索、剧情进度、镜头位置都会原样恢复。

## 常见问题

- **窗口大小不合适**：`AOE_WIDTH=逻辑宽 ./run.sh`（如 480/720/960；高度固定 320），
  窗口倍数用 `-Daoe.scale=N` 微调。
- **菜单/剧情画面右侧有黑边**：原版 UI 素材是按 240 宽画的，宽视野下暂未做
  适配（任务内的战场视野不受影响），后续版本处理。
- **右键没反应**：右键移动需要先选中单位（左键点单位，或拖动框选）。
- **按了 0 出现整屏蓝色+黑色菱形**：这就是小地图，黑色是未探索区域，
  不是死机——再按 `0` 返回。（旧版有两个"按 0 后一直出不去"的 bug：
  连发键误判、以及剧情对话框弹回蓝屏，2026-09-01 均已修复。）
- **玩着玩着画面冻结**（键鼠似乎还有响应但画面不动）：2026-09-01 修复了已知的
  触发源（第一场远程战斗时投射物瞄准扫描死循环，复盘见 WORKLOG.md）。
  游戏内置卡死看门狗：若再发生，`grep watchdog` 最新一份 run-*.log 可直接看到
  卡死位置，发来即可。

## 反馈问题

每次运行都会自动留一份日志在
`~/Library/Application Support/AoeJ2ME/logs/`（保留最近 10 份）。
报问题时把出事那次对应的 `run-*.log` 发来即可定位。
