# 自对弈竞技场（Self-play Arena）规格与协议

2026-09-07 立项（用户拍板）。目标：在对称、公平、不作弊、不开全图的自对弈
竞技场里持续迭代提升 AI 强度。本文档是竞技场规格 + 评估协议 + 联赛表的
唯一权威；每轮迭代的结果登记在末节联赛表。

## 第 0 条铁律（用户拍板，凌驾一切）

**默认游戏行为永远不变**。所有研究性改动都是可选分支（旋钮默认关/新类/
新 hook），不设旋钮时默认路径逐字节等于原版。敌 AI 默认永远是引擎
tickAi；enemyAi、fair 旋钮、arena 模式全部是显式挂载才生效的实验开关。

## 竞技场规格（`-Daoe.arena=1` 主开关捆绑以下全部）

| 项 | 规格 | 实现 |
|---|---|---|
| 参赛双方 | 两侧都是 RuleBasedAi（`-Daoe.playerAi` + `-Daoe.enemyAi`） | 已有 |
| 起始资源 | 双方拉平 200/100/100 | `fairStart`（arena 隐含） |
| 采集乘数 | 双方 ×1（无作弊） | `fairGather`（arena 隐含） |
| 资源滴 | 无 | tickAi 抑制 + 不开 enemyDrip |
| **迷雾** | **两侧都诚实**：side 0 用引擎迷雾层（mapTiles 0x8000）；side 1 用 AI 自维护的私有探索 bitmap（按自己单位/建筑每 tick 揭 3×3/半径 3/塔 6，镜像引擎语义） | side1 私有雾（第 0 轮已落地） |
| 调用顺序 | 帧首两侧 AI 的调用序按 tickCount&1 交替（消固定先手） | c.java onPaint（第 0 轮已落地） |
| 模拟段顺序 | tickUnits/tickAutoEngage/aimProjectiles/tickProjectiles/tickBuildings 的玩家处理序按 tickCount&1 交替（与帧首 AI 序同奇偶），中和"恒 P0 先行动"的占位竞争/先开火/先索敌 | `arenaSimAlt`（arena 隐含开，=0 回退；第 1 轮落地） |
| 出生对称 | 原版 Easy/Medium（randomMapDifficulty<2）只给 P0 白送侦察骑兵（type 5）；arena 下给 P1 镜像位补一只 | `arenaSymSpawn`（arena 隐含开，=0 回退；第 1 轮落地） |
| 遥测 | arena 下每 500t 打 `[arena] t=… s0[u/d/mv/r/exp] s1[…]` 对称指标行（side0 exp 排海：(tile&0xFFF)==768 从不置雾不计入） | c.java arenaTelemetry（第 1 轮落地） |
| side 偏差控制 | **镜像配对**：同种子 candidate 在 side 0 / side 1 各赛一局，合并计胜率 | ailoop `-m`（第 0 轮已落地） |
| 认输 | 竞技场模式下 side 1 的僵尸投降门开启（打 `[ai] concede side=1`；exitOnResult 批测路径再打 `[result] WIN`+exit=player 0 胜，非批测只打日志、终局仍走引擎） | 第 0 轮已落地 |
| 和局 | tickCount > 50000 判和（≈当前对局均值 2.7 倍），`[result] DRAW` + exit（仅 arena+exitOnResult 批测路径；隐藏覆盖旋钮 `aoe.arenaDrawTick`，默认 50000 勿改） | 第 0 轮已落地 |
| 旋钮按侧 | RuleBasedAi 全部自消费旋钮（aiK.*/exm.*/exp*/spNear/aiFog）支持 `aoe.<name>.p0/.p1` 按侧覆盖，不设则与 base 名/默认值逐字节一致 | 第 0 轮已落地 |

## 候选评估协议（防双生过拟合是命门）

1. **候选 = 旋钮门内的新机制**。旋钮必须支持**按侧覆盖**：`aoe.<name>` 为
   两侧默认，`aoe.<name>.p0`/`aoe.<name>.p1` 覆盖对应侧（镜像配对靠它——
   同一 JVM 内 candidate 与 champion 各居一侧）。
2. **镜像批**：ailoop 镜像模式，n=20（10 种子 × 双侧各一），同形状
   phase pin。candidate 胜 ≥14/20（二项 p≈0.08）才算赢。
3. **引擎锚**：candidate（两侧同开）对引擎敌 AI 跑 Easy/Medium/Expert 各
   一批 n=10，相对当前 champion 基线显著回退（>2 胜场差）则不采纳——
   防止"只克自己镜像"的策略。
4. **联赛制**：历代 champion 以旋钮组合存档于末节联赛表；candidate 还须
   对上一代 champion 配置不落下风（同协议镜像批）。
5. **退化监控**：报告平均 ticks；胜率涨但 ticks 翻倍的"拖死型"策略不算
   变强（个案裁决）。
6. 判死方向登记进联赛表「别再试」区，附证据。

## 迭代循环（每轮 ≈ 一个 sub-agent 夜）

上轮败局尸检（ailoop `-S` 快照 + `[ai]` 日志）→ 提炼一个假设 → 旋钮门内
实现 → 镜像批 + 引擎锚批 → 采纳（旋钮转正）/判死（登记）→ 联赛表 +
WORKLOG。

## 假设储备池（按预期收益排序，每轮取一个）

1. ~~探图速率~~（第 2 轮已试，判死登记见末节：scoutRay 干预中性 10.5/20；
   第 3 轮归因修正：原归因"早攻撞塔环负和"被证伪——先攻在对称局实测正和，
   中性=纯提速+双方同受益。复测前提"攻击门重调"已作废）
2. ~~波 1 后产能链韧性~~（第 4/5 轮已试，判死登记见末节：probeStride=4 镜像批
   13/20 未过线；第 5 轮按复测前提做组合 probeStride=5（stride-4 发现半 +
   村民轨道卡死门）= **13.5/20 仍未过线，probeStride 家族到此为止**。败因测绘
   结论：镜像局产能分叉不是"射箭场时机"（45 夜映射
   不同构——村民恒 4 引擎帽、双侧同码同时点放建筑），而是**单资源盲区→收入
   0→军容/军值冻结**（金×3、石×2 = 5/10 种子）；第 5 轮标定进一步证伪"细流
   金图"（resDbg 全图真值：镜像图资源格几乎全 ≥15 趟），真残余约束 = 几何层
   （袋形格轨道卡死、TC 围死交存、袋形矿封回程）+ 战斗层早战 + 军容帽噪声）
3. ~~承伤结构微操~~（集火 43 夜已收；**第 6 轮（2026-09-07）进攻态会战质量
   方向判死**，诊断+天花板量化见末节「别再试」：HUNT 同刻对撞的交战质量影响面
   只有 1003/1006（+边际 1005），乐观上限 12.5-13/20 < 14 采纳线，未做
   candidate；C 类 3 种子真约束两个在 TC 发现/盲游不在交战）
4. ~~攻城时机学（敌塔环密度 vs 己方投石机数的出击判据）~~（第 8 轮已试，
   **反向证伪判死**：t8<敌可见完工塔的总攻 6/6 全胜，t8≥塔反 12W-4F；唯一
   撞塔失败帧 t8≥塔、假设的门会放行；brief 变体触发点 0/20 局。登记见末节）
5. ~~旋钮空间自动爬山~~（第 7 轮已试：harness 落地可用，但首轮 13 评估的
   两个采纳点独立复测全是噪声——单基因边际在镜像局是噪声级；转低频工具，
   有新 champion 或改大样本评估（n≥40/点）时再爬）
6. ~~攻击触发门的对称局重校~~（第 3 轮闭环判死：对称局先攻不负和——
   G0 批先攻方胜率 60%、scoutRay=1 复测批真先攻（领先>500t 非 HUNT）8/10=80%，
   攻击门无需重校。唯一负和触发器是 TIMERUSH 0/4，量级不足单列假设）

## 基建清单（第 0 轮，2026-09-07 落地）

- [x] RuleBasedAi 旋钮按侧覆盖机制（静态 final → 构造期按 side 解析实例字段）
- [x] side-1 私有迷雾 bitmap + fogHonest 对 side 1 解锁
- [x] ailoop 镜像模式（`-m`：同种子双侧各一，candidate 旋钮按侧分配）
- [x] 帧首 AI 调用序按 tick 交替（arena 门内）
- [x] side-1 arena concede + DRAW 规则
- [x] 公平性确认批：对称竞技场的 side 0/1 胜率基线（镜像合并后应≈50%）

第 1 轮（2026-09-07）追加：

- [x] **side 偏差定位与中和**（见联赛表 G0 行注）：两个偏差源实证——
  ① 出生不对称：原版 Easy/Medium 只给 P0 白送侦察骑兵（type 5），
  遥测 t=0 s0 u=3/mv=9 vs s1 u=2/mv=4；② 模拟段恒 P0 先行动（tickUnits
  占位先到先占/resolveAttack 先杀/tickAutoEngage 先索敌/投射物先落地）。
  中和 = `arenaSymSpawn` + `arenaSimAlt`（均 arena 隐含开）。效果：
  80% → 65%（仅 symSpawn）→ 55%/35%/50%（组合，三种子集各 n=10×2），
  合并 60 局 side0 = 28/60 = **46.7%**（落入 [40%,60%] 噪声带，残差无系统
  方向——seed 1010 同图同配置仅相位不同即翻转胜方）。
- [x] 假设②（渲染期雾泄漏）证伪于批测语境：ailoop 批跑 noRender=1 整跳
  renderWorld，该路径不存在（GUI 独有，且对双方 owner 都揭）。
- [x] `[arena]` 500t 遥测（对称指标分叉时点判据）。

第 7 轮（2026-09-07）追加：

- [x] **旋钮空间自动爬山 harness**（`tools/arena-climb.py`，纯工具层/stdlib，
  零游戏代码改动）：14 维基因型（aiK.milVills/meleeW/meleeG1/archW/
  towerW/towerG/towerS/towerCap/smithW/smithS/t8w/t8g/bowsawW/exm.focusD2，
  默认值=G0，步长表内置于脚本 GENES），适应度=ailoop 镜像批
  （`ailoop.sh -m <gene>=<value> -n 10 -d 2 -k -b`，champion=当前基因型经
  EXTRA_D 双侧 base 透传，candidate=单基因变异）的 candidate 合并胜场
  （DRAW 计半，满分 20，镜像自对弈平局基准恒 10.0）。坐标下降：每基因
  试 ±步长，>10.0 即采纳最高分者，一轮无改进或预算（默认 75min）耗尽
  自停写 summary。状态（基因型/评估缓存/历史）持久化
  `<workdir>/state.json`（默认 /tmp/arena-r7），缓存 key = (champion 基因型,
  基因, 候选值, 种子集) 的 sha1——同种子同相位 pin 下同 key 结果确定，
  杀掉重启自动续跑不重跑。用法：
  `nohup python3 tools/arena-climb.py --minutes 85 > /tmp/arena-r7/climb.log 2>&1 &`
  （冒烟：`--games 1 --max-evals 2 --workdir /tmp/arena-r7-smoke`；
  单实例锁 climb.lock；收尾写 `<workdir>/summary.txt`）。
  采纳线（第 10 轮定稿）：≥11.5（噪声边际 1.5——首轮实证 10.0 线会把噪声
  当改进，meleeW/towerG 两采纳点复测 21/40 全军覆没）；产出仍须按协议
  独立复测（≥28/40）+ 引擎锚不显著回退才转正。

## 联赛表

| 代 | 配置 | 镜像成绩 | 引擎锚 E/M/X | 日期 |
|---|---|---|---|---|
| G0 | 基线 RuleBasedAi（科技对称化后） | **第 0 轮（中和前）**：side0 得分 16.0/20 = 80%（10 种子×2，逐种子 7W-1L-2D；ticks 均 26649）。**第 1 轮中和后（arenaSimAlt+arenaSymSpawn 隐含开）**：side0 = 11.0/20=55%（种子 1000+）、7.0/20=35%（1010+）、10.0/20=50%（1020+），合并 60 局 **28/60=46.7%**——side 偏差已中和进噪声带；后续 candidate 镜像测量直接受益（残差 ±15pp/10种子 级为图运噪声） | 6/10、0/10、0/10（46/47 夜口径=对引擎敌 AI 的 side 0 胜率） | 2026-09-07 |
| **G1** | G0 + **aiK.towerS 16→20**（塔门石抬 4） | 爬山 R1 唯一采纳点（14.0/20，r10 workdir）；**独立复测 28.0/40 恰线**（cand@p0 批 side0=17/20、cand@p1 批 side0=9/20，rundir 213139/213806）——cand@p0 批强烈、@p1 批中性，净值达标 | **10/10、8/10、1/10**（对照 G0 同形状口径 8/8/1：Easy +2、Medium 平、Expert 平——无回退，rundir 214548/214829/215114） | 2026-09-07 转正（第 10 轮） |

第 10 轮（2026-09-07，有界演化实验=修正采纳线后的爬山重启，**项目首个
转正 candidate**）：arena-climb 从 G0 重启 175min/27 评估（坐标下降完整
R1），ADOPT_SCORE=11.5 下唯一采纳 towerS 16→20 @ 14.0/20（上轮噪声采纳
点 meleeW 10.5/towerG 11.0 全部被新线正确放过）。⚠️ 读数纪律：多个基因
评估 ticks 完全相同（smithW/S、towerCap、focusD2 全 19937）= **旋钮不绑定**
（门在这些局从不触发），是无信息不是中性证据。机制解读：塔门石 16→20
=放塔更谨慎（多攒 4 石缓冲），对撞期少一座半成品塔多一分军容——与
「攒没攒够才是杠杆」的八轮总结一致。转正后 G1 成为新 champion，ailoop
历史基线（塔门 16 口径）自此不可比，逐种子基线需重录。

第 0 轮的 side 偏差假装备查已在第 1 轮闭环：① 坐实（模拟段恒 P0 先，
arenaSimAlt 中和）；② 证伪（批测 noRender 无渲染路径）；③ 真凶补获=
出生侦察兵不对称（arenaSymSpawn 中和）。镜像配对协议仍是 candidate 评估
的统计底座（n 有效=10 的图运噪声靠它摊薄）。

第 3 轮（2026-09-07，攻击触发门对称局重校假设，**归因证伪提前收官，无
candidate**）：① 日志卫生落地——RuleBasedAi 全部 41 处 [ai] 日志加行首
`s0`/`s1` 侧标（纯文本变化：regress 三连+replaycheck+无 enemyAi 基线 5 局
逐种子一致+G0 镜像复跑 20 局 ticks 与 090155 批逐字节一致、剥 sN 前缀后
[ai]/[result] 流一致）。② 先攻方胜率诊断：G0 批 12/20=60%（8/10 种子双侧
同刻 HUNT@15k 无先攻方；真先攻 2/4：TIMERUSH 0/2、CRUSHED+OVERWHELM 2/2），
scoutRay=1 复测批 13/20=65%、真先攻 8/10=80%——**第 2 轮"先攻撞塔环负和"
归因证伪，攻击门对称化重校（储备池第 6 项）闭环判死**。rundir：诊断 G0
20260907-093148、scoutRay 复测 20260907-094258（复现第 2 轮 10.5/20）。

第 4 轮（2026-09-07，败因测绘 + 产能链韧性假设，candidate probeStride=4
**13/20 未过线判死**）：① 镜像局败因结构表（G0 批 093148 全量日志测绘）：
A·单资源盲区产能锁死 5/10（金×3：1000/1006/1007；石×2：1008/1010）、
C·对称会战/HUNT 盲游 3/10（1003/1005/1009，1009=强侧 22/292 未找到敌 TC
盲游丢家）、B·近距早战消耗 1/10（1002）、D·TIMERUSH 1/10（1001）。
**45 夜"胜者 ≤3.4k 开射箭场"映射在镜像局不同构**：村民恒 4（引擎 hdr[75]
帽）且双侧同码同时点放建筑，产能不分叉于建筑时机，分叉于单资源盲区→收入
0→军容/军值冻结（金=全兵种停产；石=塔链/射箭场/马厩全锁军容帽 10-15）。
② candidate `aoe.probeStride`（默认 0=逐字节现行为，按侧可覆盖）：资源
盲区专职探员螺旋游标 ×N 步进采样（time-to-radius ÷N）+ 解锁石盲区抽探员。
机制生效实锤：金发现 16130→6554（1007）、石 0→1513/7559（1008/1010）、
5 种子有利翻转 vs 1 不利；镜像批 p0 8-2 / p1 5-5 合并 13.0/20（rundir
20260907-104037），ticks 均 17251（-17%）。未翻种子尸检：残余约束=4 村民
零和下木瓶颈（石被发现→配额抽走木工→W=0 反更差，1008）、细流金图（发现
≠收入，g2 全程 G≤26，1000）、马厩军容差（1007）。判死登记见末节。

第 5 轮（2026-09-07，组合假设：probeStride=4 发现半 + 消费侧闸，
candidate probeStride=5 **13.5/20 未过线判死，probeStride 家族终线**）：
① **标定先推翻简报前提**——新诊断旋钮 `aoe.resDbg`（默认 0；=N dump 全图
资源趟数直方图+resmap 透雾全图，=2 每 500t 逐村民 vtrace）全图真值实测：
镜像图资源格几乎全 ≥15 趟（seed 1000 金簇 24-27,41-45 全 31 趟且 BFS 洪泛
从 TC 可达）——"细流金图发现≠收入"证伪，细流门（趟数 <4 视同盲区）写成
后又移除。② **vtrace 抓到真凶**：1008 的 W=0 不是配额漂移——v1 目标
56,41（木）被 BFS 归队/扇形振荡困在 (51-55,36-38) 轨道 14k tick（pos 恒变，
按位置不变的 300t STUCK 检测永远够不着——R2 侦察兵 parking 同族 bug 的
村民版）；1000 的金零收入=矿簇袋形/矿场封喉回程不可达（几何层）。③
组合实现：=5 = stride-4 发现半逐字节复用 + **村民轨道卡死门**（同一资源格
目标连续行军 >2400t 且距离²>9 → 拉黑 3000t+立即重派；时钟只计连续行军
状态——批 1 事故：累计时钟版把健康矿工跨趟累计 >2400t 连环误拉黑 9 格
经济雪崩，1001p0 翻负 13→12/20；修连续口径后批 2 恢复 1001p0 逐 tick 轨迹
WIN@10749）。批 2 = **13.5/20**（p0 8-2、p1 5-4-1，rundir 20260907-123004）：
vs R4 净 +0.5（1009p1 L→D，HUNT 盲游拖成 5w 和局）；1008 木收入救活
（W 0→960@16000、u 11→19、mv 86→247）但军事差距是前 2000t 落下的，未翻转。
④ CARRY-STUCK 变体（携带态 2400t 未卸货=交存点围死→重派弃载；1008 v0
背石绕 TC 14k tick S=6 实锤）批 3 = **12.0/20 有害回滚**（弃载断送在途载荷
+搅乱岗位：1008p0/1009p0 翻负、1009p1 D→L；未点火局 ticks 与批 2 逐字节
一致=归因干净）。⑤ 微候选验证：对称局关 TIMERUSH（`aoe.noTimeRush=1`）
镜像批 **10.0/20 与 G0 完全打平**——快败变慢败（1001p0 1903→16878t）零
翻转，"TIMERUSH 负和"止步于拖长败局，登记别再试。判词：发现提速+消费侧
零和闸封顶 13.5，残余约束全在几何层（袋形格/交存围死/矿场封喉）与战斗层
——配额/消费侧的门够不着，probeStride 家族不再续投。

第 6 轮（2026-09-07，HUNT 同刻对撞会战质量假设，**天花板量化不足提前判死，
未做 candidate**）：① 诊断基建——新旋钮 `aoe.battleDbg`（默认 0 纯日志，
=1 每 500t 打双侧兵种构成/残血/攻击态/军事质心的 btl 行），诊断批
（20260907-133900）20/20 逐种子 ticks 与 130916 G0 基线逐字节一致=零行为差。
② 会战测绘（10 种子全表，构成+死亡交换比+[combat] 死亡序列）：C 类 3 种子
里只有 1003 是均势会战（10v10 构成镜像、8:8 交换，胜负在 s1 冲塔失败+s0 晚
找到 TC）；1005 是双盲 50k 找不到 TC 的和局（野战已分胜负也转化不了）；
1009 是强侧赢野战（13:7）却盲游丢家（败因=TC 发现/回防时机，DEFEND/ABORTED
机制工作正常只是军队太远）。**等构成等数量会战崩溃的唯一样本=1006**
（15v15 构成镜像，开阔地塔程外交战 16:5——[combat] 序列实锤 s0 行军纵队被
s1 集团抓了以少打多+3 猫全死在敌堆；接战几何而非纪律参数）。③ brief 问题
闭环：进攻态（attackMode 含 HUNT/总攻）的 150t 重发 = 全军事群令同一格
裸冲——远程无停射位（DEFEND 非 Expert 分支有 3 格停射位）、无集火
（EXM_FOCUS 只在 Expert DEFEND 门内，镜像批 Medium 不生效=镜像会战双侧
全裸）、每 150t 清全军装填+打断 reprisal 追击（对撞期 act1 快照仅 3-7/15）。
缺口真实存在但影响面=1003/1006（+边际 1005），乐观上限 12.5-13/20 < 14，
按预登记规则判死。④ 败因结构修订：C 类主约束=TC 发现/盲游（2/3），
会战质量连 C 类都只能管一半。

第 7 轮（2026-09-07，旋钮空间自动爬山——harness 落地 + 首轮搜索 +
产出复测判死）：① harness `tools/arena-climb.py`（用法见基建清单末节）：
14 基因坐标下降，champion=活基因型（EXTRA_D 双侧透传），确定性缓存 +
断点续跑。② 首轮搜索（85min，13 评估=坐标半轮）：采纳 meleeW 15→20
（10.5）与 towerG 6→9（11.0），两个都在噪声带内。③ **独立复测（协议
强制）判死**：组合基因型双侧分批（EXTRA_D 按侧覆盖，-m - 镜像）——
cand@p0 批 side0=13.0/20、cand@p1 批 side0=12.0/20，candidate 合计
**21.0/40=52.5% < 28/40 采纳线**，两个采纳点全是噪声（harness 作者
预警的噪声采纳风险实锤）。④ 协议修订：harness 采纳线 10.0→**11.5**
（+噪声边际），产出仍须复测。判词：14 维门阈值空间的单基因边际在
镜像局都是噪声级——坐标下降的方向本身存疑，若再投应改大样本评估
（n≥40/点）或换演化策略；登记后储备池⑤转入低频使用（有新 champion
时再爬）。

第 8 轮（2026-09-07，攻城时机学假设——总攻按"投石机 vs 敌塔环"火力交换比
门控，**反向证伪提前判死，未做 candidate**）：① 诊断基建——新旋钮
`aoe.siegeDbg`（默认 0 纯日志，按侧解析）：ATTACK enemy TC / HUNT 中首见敌
TC（TCFOUND=猎寻转总攻快照）/ STALLED / ABORTED / RETREAT 五处打 siege 行
（我方 t8 数 / 敌完工塔 etw=可见+全图真值 / 双方军容军值 / 敌塔军值 twval）。
诊断批（20260907-170313，`-m - -n 10 -d 2 -k -b` + EXTRA_D）**20/20 逐种子
ticks 与 130916 G0 基线逐字节一致**=零行为差。② 总攻测绘（22 episodes/20 局，
去镜像重复 11 个：ATTACK 直发起 3 + TCFOUND 转总攻 8；1005 双盲局无总攻，
HUNT 态 abort/retreat 不计入）：**t8 < 敌可见完工塔的总攻 6/6 全胜**
（1001s1 反杀/1006s1 冲 4 塔/1008s0 冲 3 塔，各×镜像 2；真值口径 7W-1F），
**t8 ≥ 塔的反而 12W-4F**；Δmv500 无分离度（胜方 -18/+8/+12，败方 -16/-16）；
STALLED（塔环挡住总攻的停滞撤军）全批 0 次——"撞塔环负和"病理在镜像基线
不存在。③ 机制解释：镜像局总攻全是晚期（16k-29k tick）军值已定局的收割
（攻方 val 138-292 vs 守方军事 0-18 + 塔 17-69），塔环是收尾变量不是胜负
变量；第 6 轮关键帧 1003 s1 冲 2 塔失败恰恰是 t8(3)≥etw(2)——假设的门会
**放行**它，败因是 9 兵 val 108 撞 6 兵+2 塔的兵力不足而非火力交换比。
④ brief 拟的 candidate 变体（etw≥2 且 t8==0 不进总攻）在 20 局里**触发点=0**
（t8==0 的 episode 仅 1001 双侧，且可见塔 0/1）——天花板 10.0/20 < 14，
按预登记规则判死。分析脚本 /tmp/arena-r8-analyze.py，笔记 /tmp/arena-r8-notes.md。

第 9 轮（2026-09-07，交存点几何假设——AI 侧落位纪律：营地可达性验证/TC 呼吸
半径/矿场朝向，**天花板 11/20 判死，未做 candidate**）：① 诊断基建——新旋钮
`aoe.geoDbg`（默认 0 纯日志，按侧解析）三通道：携带态 trip 跟踪（action==3 且
高半字节!=0=背货回程；close 分类 SEALED>2400t/DETOUR 行程>2×直线+4/ABORT
未到站/ok 静默；用 trip 内最小 Chebyshev 判到站，close 时 pos 判会全误 ABORT——
卸货后 8t 内已走开）+ 每 500t geo 摘要（TC 围死度 tcb=Chebyshev≤2 环 24 格
建筑数+正交 8 格子集/营地锚位/trip 计数）+ 营地落位瞬间 camp 行（锚点资源格↔
落位格可走邻格 BFS 距离，bfs=-1=放下即不互通）。诊断批（20260907-175453，
`-m - -n 10 -d 2 -k -b` + EXTRA_D）**20/20 逐种子 ticks 与 171435 G0 基线
逐字节一致**=零行为差。② 测绘结果三箭全空：**营地落位不可达 0/40**（bfs 最大
3，"矿场放下即封喉"触发率零——候选①胎死）；**TC 围死度无胜负分离**（败方
5-9/24 vs 胜方 8-14/24，1003 胜方 tcb=14——候选②无据）；携带态病理双侧都有
且胜方照样赢（1000 s0 胜方 25 次金 DETOUR=袋形振荡每趟 trav20-36/st5-7、
1008 s0 胜方 2 次 SEALED 10.7k tick——经济体量吸收得住）。③ **第 5 轮归因
证伪**：1000 s1（败方）金零收入窗口（8k-15k）里矿场 22,40 锚 24,42 **bfs=1
可达且全程在位**（7280 放到 ~17250 才被拆），s1 金 carry trip 零异常——真机制
是袋形簇双侧共采（双方矿场 22,40/25,46 贴同簇）的采集循环打断/重派流失
（v13 蹲守-逃命环同族，Medium 镜像无 camped 标记机制），不是"回程不可达"。
④ 天花板：唯一几何可及败局=1008 s1（v1 背石朝 TC 61,37 parked 13.3k tick=
1/4 劳动力+11 小绕行，det+seal 占比 14%；且封因可能是单位拥塞/地形非建筑，
tcb8=4）乐观 +1 → **11/20 < 13/20 判死线**，按预登记规则判死，未烧候选批。
分析脚本 /tmp/arena-r9-analyze.py，笔记 /tmp/arena-r9-notes.md。

### 别再试（判死登记）

- **交存点几何/建筑落位纪律方向——营地落位 BFS 可达性验证、TC 呼吸半径、矿场
  朝向（第 9 轮，2026-09-07，诊断判死未做 candidate）**：见上第 9 轮注记②③④。
  一句话：镜像批 10 种子里营地落位不可达 0/40（候选①触发率零）、TC 围死度
  胜负无分离（胜方 tcb 反而更高）、携带态绕行/封喉病理胜方照样吸收（1000/1008
  胜方带 25 次 DETOUR / 2 次 SEALED 照赢）；第 5 轮"1000 矿场封喉回程不可达"
  归因被证伪（矿场 bfs=1 可达全程在位，真机制=袋形簇双侧共采的采集循环打断）。
  唯一几何可及败局 1008 s1 乐观 +1 → 天花板 11/20 < 13/20。**几何层（落位侧）
  不是镜像局 A 类败因的 binding constraint，别再试**；诊断旋钮 `aoe.geoDbg`
  默认关留码（carry/geo/camp 三通道尸检直接复用）。A 类残余约束最终格局：
  发现时机（probeStride 家族封顶 13.5）+ 蹲守逃命环（Medium 无 camped 机制）
  + 前 2000t 军事时间线——全部已登记。

- **攻城时机学方向——总攻按"t8 vs 敌塔环"门控（第 8 轮，2026-09-07，诊断
  反向证伪判死未做 candidate）**：见上第 8 轮注记②③④。一句话：镜像局里
  攻城火力不足的总攻全胜（6/6）、火力够的反而有败（12W-4F），假设的选择
  方向是反的——按它做门会拦下 6 场胜局、放行唯一的撞塔败局。总攻时机在
  镜像局不是杠杆：发起时胜负已被军值差定局（第 3 轮"先攻正和 80%"+本轮
  "撞塔不输"互证——**对称局进攻侧的一切时机学微调都别再试**）。诊断旋钮
  `aoe.siegeDbg` 默认关留码（siege 行含 etw 可见/真值双口径，尸检直接复用）。


- **进攻态会战质量方向——远程停射位/进攻集火/t8 倾斜（第 6 轮，2026-09-07，
  诊断判死未做 candidate）**：进攻态"裸冲"缺口实锤存在（见第 6 轮注记③），
  但逐种子天花板账户：镜像基线每种子恒贡献 1 分，battle-quality 杠杆可及翻转
  = 1003 cand@s1（s1 冲 2 塔失败，远程停射位理论可翻，边际）+ 1006 cand@s0
  （等构成会战 16:5 崩溃，但 s0 金盲区、野战后还有 vs 金收入方的重建竞速）
  + 1005 至多半分（野战可翻、TC 找不到转化不了）；1009（败因=盲游丢家非交战）
  与 A 类数量碾压局（1.5-3.5×）与 B/D 早战局全部够不着。**乐观上限
  12.5-13/20 < 14 采纳线**。诊断数据与机制证据：/tmp/arena-r6-notes.md +
  rundir 20260907-133900（battleDbg btl 行可复用）。若未来要复活此方向，
  前提是先解决 C 类主约束（TC 发现/盲游）使会战成为 binding constraint。

- **探图速率方向——scoutRay 侦察链修复/射线强化（第 2 轮，2026-09-07）**：
  相关性验证成立（G0 镜像批 exp 领先方 t=500 胜率 8/9、t=2000/4000 均 7/9，
  6/9 局在 0 接触前即分叉=非赢家滚雪球；敌 TC 发现与胜利强耦合：单 SCOUT 局
  全是"发现后 ~300-500t 终结"，双 SCOUT 局 5/5 先发现者胜）。但干预中性：
  卡死修复（`aoe.scoutRay=1`）与全包（=2：修复+射线多遍重扫+再锚定放宽）
  镜像批同为 **10.5/20**（p0 4-6、p1 6-3-1；基线 10.0/20）——远低于 14/20
  采纳线，且两变体同分说明全部效果来自修复本身。**第 3 轮归因修正（原
  "先攻撞塔环负和"机制教训证伪）**：第 3 轮用带 s0/s1 侧标的 [ai] 日志
  直接测量先攻方胜率——G0 批 60%（含同刻 HUNT 平局任意归 s0；真先攻
  2/4），scoutRay=1 复测批 65%，其中**真先攻（领先 >500t 且非 HUNT）
  8/10=80% 胜**：OVERWHELM 3/3、CRUSHED 系 3/3、DESPERATE 1/1、
  GOLDSTARVE 1/1，且攻击前后 500t 军值几乎不掉（186→186、165→165、
  155→163）——先攻撞塔环负和不成立，攻击门在镜像局是正和的。唯一负和
  触发器是 **TIMERUSH 0/2**（两批合计 0/4：47→24 撞完被 CRUSHED 反杀，
  seed 1001 型）。scoutRay 中性的真实解释 = 纯提速（均 ticks
  20688→17836，-14%）且双方同受益、胜率结构不变；10.5 vs 10.0 在 n=20
  噪声带内。原"复测前提（攻击门对称化重调）"作废——攻击门不需要重调。
  **副产品（真 bug 定位，保留在
  旋钮门内）**：侦察链"卡死 300t 跳路点"是死代码（scoutLastT 每决策无条件
  刷新→门永假；且 BFS 重寻路使被卡单位 2×2 抖动，按位置判 stationary 也
  不可靠）——双侧侦察兵在不可达路点 parking 到 15k HUNT 收编（seed 1006
  SCOUTDBG 实证）。修复后敌 TC 发现 24755/28947→5307/5667（提前 ~19k）。
  探图速率若想再回来，候选应直接测胜率，不要指望"先修攻击门"联动。
- **资源发现提速方向——probeStride 专职探员跳环（第 4 轮，2026-09-07）**：
  相关性成立（败因测绘：5/10 种子败因=单资源盲区产能锁死，机制链=资源未
  入探→findResource 只认已探索格→无矿场/无派工→收入 0→军容军值冻结→
  15k HUNT 对撞被碾；专职探员螺旋发现半径 R 代价 ∝ R²×100t，r=15 ≈ 13k
  tick，seed 1007 s1 金距 TC ~12 格 15.8k 才发现实锤）。干预部分生效但
  未过线：`aoe.probeStride=4` 镜像批 **13.0/20**（p0 8-2、p1 5-5；采纳线
  14/20）——发现延迟实锤大降（金 16130→6554、石 0→1513），但残余约束封死
  上限：① 4 村民零和（引擎 hdr[75]=4 帽）下"多发现一类资源=抽走木工"
  （1008 石@1513 后 W=0 全程，mil 7/55 比 G0 更差）；② 细流金图发现≠收入
  （1000 g2 全程 G≤26）；③ 军容帽差（1007 对方马厩 21/278）。scoutRay=1
  （10.5/20）→probeStride=4（13/20）同方向递进，两个变体证明**单纯发现提速
  封不到 14/20**。~~复测前提：与配额/消费侧机制组合~~——**第 5 轮已兑现并
  判死（家族终线）**：probeStride=5 组合档（stride-4 发现半 + 村民轨道卡死
  门：同一资源格连续行军 >2400t 且距离²>9 → 拉黑+重派）镜像批
  **13.5/20**（p0 8-2、p1 5-4-1，rundir 20260907-123004），仍 <14。第 5 轮
  resDbg/vtrace 标定把第 4 轮尸检的两个"残余约束"证伪/改写：细流金图不成立
  （全图真值资源格几乎全 ≥15 趟）；"配额抽木工→W=0"真因=村民轨道卡死
  （BFS 振荡 pos 恒变骗过 300t 位置不变检测，R2 侦察兵 parking 同族）——
  修复后经济救活（W 0→960）仍不翻转（军事差距在前 2000t 已落成）。残留
  全在几何层（袋形格/TC 围死交存/矿场封喉回程）与战斗层，消费侧的门够不着。
  **probeStride 家族（=1/=4/=5）到此为止，不要再续投。**旋钮默认关留码
  （含石盲区触发 pullStone、=5 轨道门；附诊断旋钮 `aoe.resDbg`：=N 全图资源
  趟数+resmap 透雾 dump，=2 逐村民 vtrace，标定/尸检直接复用）。
- **村民携带态卡死重派（CARRY-STUCK，第 5 轮，2026-09-07）**：携带态连续
  2400t 未卸货（=交存点围死，1008 v0 背石绕 TC 14k tick S=6 实锤）→ idle
  重派弃载。镜像批 **12.0/20 有害**（弃载断送在途载荷+搅乱健康岗位：1008p0/
  1009p0 翻负、1009p1 D→L），已回滚不留码。教训：围死局的正确解在交存点
  几何（建筑落位间距），重派弃载只是把"永久锁死"换成"周期性白跑"。
- **对称局关 TIMERUSH（aoe.noTimeRush，第 5 轮，2026-09-07）**：第 3 轮登记
  的唯一负和触发器（两批 0/4）。镜像批 **10.0/20 与 G0 完全打平**——快败
  变慢败（1001p0 1903→16878t）零翻转。负和证据止步于"拖长败局"，不改变
  胜率结构。旋钮默认关留码，别再单独复测。
